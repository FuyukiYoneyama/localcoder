# LocalCoder 完全再構築ガイド（移植用）

> 文書の役割: 別PCへの移植、完全再構築、詳細な検証手順を扱う。現在の機能一覧や日常の起動方法は README.md、変更の時系列は CHANGELOG.md を参照する。

このガイドと同一コミットのリポジトリ一式があれば、AI（Claude Code / codex 等）または人間が別のPCに
**LocalCoder** — ローカルLLM(Ollama)だけで動くGUIコーディングエージェント —
を完全に再構築できる。外部LLM APIキー不要、Pythonの外部ライブラリ不要。

> **AIへの指示**: この文書と必ず同一コミットのリポジトリを使用すること。「1. 前提条件」を確認後、
> 「3. ファイル一式」に従って配置し、「4. 検証手順」を実行する。環境差分は「5. 環境差分の調整」を参照する。

---

## 1. 前提条件

| 項目 | 要件 | 確認コマンド (PowerShell) |
|---|---|---|
| OS | Windows 10/11 + WSL2 (Ubuntu系) | `wsl -l -v` |
| Ollama | Windows版 0.12以降（0.20.2で動作確認） | `ollama --version` |
| Python | WSL内に python3（3.8以降、標準ライブラリのみ使用） | `wsl -- python3 --version` |
| モデル | ツール呼び出し対応モデル。推奨 `gpt-oss:20b` | `ollama list` |
| GPU | 8GB VRAM で gpt-oss:20b が実用速度（一部CPUオフロード） | `nvidia-smi` |

### 1-1. Ollamaのインストール（未導入の場合）

https://ollama.com/download からWindows版をインストールし、モデルを取得:

```powershell
ollama pull gpt-oss:20b     # 推奨: MoEで高速、ツール呼び出しが確実 (13GB)
ollama pull qwen3:8b        # 軽量代替 (5GB)
```

### 1-2. WSL→Windows Ollama の接続経路

WSLの `localhost:11434` からWindows側Ollamaに届く必要がある。

**方法A（推奨）: mirroredネットワーク** — **Windows 11専用**（Windows 10では
`networkingMode=mirrored` を書いても無視され、WSLはNATモードのまま動く。移植先が
Windows 10なら最初から方法Cへ）。`C:\Users\<user>\.wslconfig` に:

```ini
[wsl2]
networkingMode=mirrored
```

を書いて `wsl --shutdown` 後に再起動。確認:

```powershell
wsl -- curl -s http://localhost:11434/api/version
# → {"version":"..."} が返ればOK
```

**方法B（mirroredにできない場合）**: Windows側で環境変数 `OLLAMA_HOST=0.0.0.0` を設定して
Ollamaを再起動し、WSL内からはWindowsホストIP（`ip route show default | awk '{print $3}'`）を使う。
その場合、後述の server.py を環境変数で切り替えられる:
`LOCALCODER_OLLAMA=http://<ホストIP>:11434 python3 ~/localcoder/server.py`

⚠ `OLLAMA_HOST=0.0.0.0` はOllamaを**LAN上の全端末に公開**してしまう。AIエージェントに
作業させる場合、この変更は無許可では拒否されることがある（実際に発生した事例は
「8. 実施例ログ」参照）。**方法C（Windows 10で推奨）**として、`0.0.0.0`の代わりに
`vEthernet (WSL)` アダプタのIPだけにバインドし、ファイアウォールもそのアダプタ経由に
限定する方法が安全。詳細は「8. 実施例ログ」参照。

### 1-3. コンテキスト長の拡大（重要）

Ollamaのデフォルトコンテキスト(4K)ではエージェントが履歴を忘れて破綻する。
Windowsのユーザー環境変数に設定して Ollama を再起動:

```powershell
[Environment]::SetEnvironmentVariable('OLLAMA_CONTEXT_LENGTH','32768','User')
Stop-Process -Name ollama -Force
Start-Process "$env:LOCALAPPDATA\Programs\Ollama\ollama.exe" -ArgumentList "serve" -WindowStyle Hidden
```

（server.py側もリクエスト毎に `num_ctx: 32768` を指定するため、二重の保険になっている）

---

## 2. アーキテクチャ

```
[Edge アプリモードウィンドウ]  ← GUIに見えるが実体はブラウザ
        │ HTTP + SSE
        ▼
[WSL: server.py :8765]         ← Python標準ライブラリのみのHTTPサーバー
   ├─ GET  /                  → index.html (チャットGUI, CSRFトークン埋め込み)
   ├─ GET  /vendor/*.js       → 同梱のmarked/DOMPurify (CDN不使用)
   ├─ GET  /api/models        → Ollamaのモデル一覧を中継(モデルごとのvision対応可否付き)
   ├─ POST /api/chat          → エージェントループ (SSEでイベント配信) [要トークン]
   ├─ POST /api/stop          → 実行中断 [要トークン]
   ├─ GET  /api/sessions      → 保存済み会話の一覧 (履歴サイドバー用) [要トークン]
   ├─ GET  /api/session?sid=  → 1会話の全メッセージ (再開用) [要トークン]
   └─ POST /api/session/delete → 会話の削除 [要トークン]
        │ /api/chat (streaming, tools付き)
        ▼
[Windows: Ollama :11434]       ← ローカルLLM本体
```

**エージェントループの動作**: ユーザー入力 → LLMに tools 付きで問い合わせ →
LLMがツール呼び出しを返したら承認なしで即実行 → 結果を履歴に追加して再問い合わせ →
ツール呼び出しがなくなる（=タスク完了）まで最大80回繰り返す。

**ツール8種**: `run_command`（bash実行, cwd=作業フォルダ, 180秒タイムアウト、停止ボタン/
タイムアウトでプロセスグループごとkill）/ `read_file`（`.pdf`は`pdftotext`で自動テキスト抽出）/
`write_file` / `edit_file` /
`list_dir`（ファイル系は作業フォルダ外へのアクセスを拒否）/ `web_search`（DuckDuckGo HTML版の
スクレイピング、APIキー不要・無料）/ `fetch_url`（ページ取得→HTMLタグ除去テキスト、10KB上限）/
`view_image`（画像を読み込み、visionモデルなら次回のOllama呼び出しで実際に見せる）。
作業フォルダ自体も `$HOME`(または`ALLOWED_ROOTS`)配下でなければリクエストが拒否される。

**履歴自動圧縮の設計ノート**: 会話履歴の推定トークン数（ASCII=4文字/トークン、
非ASCII=1文字/トークンの概算）が予算（`NUM_CTX - RESERVE_TOKENS` = 24576）を超えると、
サーバーが各ループ反復の先頭で自動圧縮する。第1段階は古いツール結果の切り詰め
（直近`KEEP_RECENT_TOOLS`件は無傷、それ以前は500文字に切る。安価・LLM不使用）。
それでも溢れる場合は第2段階として、直近`KEEP_RECENT_MSGS`件を原文のまま残し、
それ以前をLLM自身に要約させて「【自動要約】」1メッセージに置換する（`compact`
SSEイベントでGUIに🗜表示）。**要約入力自体の肥大に注意**: 要約対象がnum_ctxを
超えるとollamaがプロンプト前方（=要約指示）を切り捨てて要約が壊れるため、
各メッセージを抜粋化（先頭7割+末尾3割で1000文字）した上で、入力が
`SUMMARIZE_INPUT_TOKENS`（NUM_CTX/2）を超える場合はチャンクに分割して各々要約する
（この欠陥は実際に発生し、分割要約の導入で解消した）。要約呼び出しが失敗した場合は
「【自動省略】」マーカーで単純省略にフォールバックする（文脈は失うが溢れて壊れる
よりよい）。分割境界がtoolメッセージに当たる場合は、呼び出し元assistantとペアが
壊れないよう境界を手前にずらす。検証: 冒頭に埋めた固有情報（コードネーム）が
56868→7746トークンへの圧縮を生き残り、モデルが正答することを確認済み。

**圧縮の世代劣化対策（インクリメンタル要約 + 機械抽出ファイル一覧）**: 長時間
セッションで圧縮が2回以上走ると、「古い部分」に前回の「【自動要約】」マーカー
自体が含まれ、それを毎回LLMに生ログとして食わせて丸ごと再要約すると、要約が
要約をパラフレーズし直す「伝言ゲーム」状態になり、特にファイルパスや目的・方向性
といった具体的事実が回を追うごとに薄れる問題があった。対策として`compact_history`
は圧縮対象`old`の先頭が既存マーカーかどうかを`_parse_marker`で判定し、マーカー
であればそのまま「これまでの要約」として扱って**再要約せず**、`update_summary`
（`UPDATE_SUMMARIZE_PROMPT`）が既存要約と新規分の生ログを**1回のLLM呼び出し**で
直接統合する（「既存の要約に含まれる事実は明確に古くなった場合を除き失わずに
引き継げ」と明示。当初は「新規分を要約→既存要約とマージ」の2回呼び出しだったが、
遅いローカルハードでは圧縮1回の停止時間が倍になるため1回に統合した）。さらに
変更ファイル一覧は`extract_changed_files`でLLMを介さず`write_file`/`edit_file`
呼び出しから機械的に抽出し、要約本文とは別ブロック（`--- 変更ファイル一覧 ---`）
としてマーカーに常に付記する。このブロックはLLMの出力ではなく次回`_parse_marker`で
単純にパースして引き継がれる値なので、要約の質に関わらずファイルパスが正確なまま
保持される（`build_work_state`も同じ`extract_changed_files`を使うよう統一した。
なお結果が`ERROR`の書き込みは「変更したファイル」に数えない——失敗した書き込みを
成功と報告すると、モデルが「ファイルは作成済み」と誤認するハルシネーションを
実際に誘発した）。要約自体が失敗した場合も、既存の要約とファイル一覧があれば
それは保持したまま新規分のみ省略するようにし、以前の「丸ごと省略」よりも情報を
残すようにした。

**圧縮の頻発によるストール対策（重複除去 + ヒステリシス）**: 実測で「会話が予算の
天井（24576トークン）に張り付き、数イテレーションごとに圧縮が再発して作業が
まったく進まない」セッションが発生した（73KBの履歴のうち37KBが、モデルが同じ
ファイルを読み直し続けたことによる**同一内容のツール結果の重複**だった）。
天井ギリギリで圧縮を止めるとすぐ再超過するうえ、安価な切り詰めでも履歴**前方**を
書き換えるためollamaのプロンプトキャッシュが毎回無効化され、全プロンプトの
再処理（CPUオフロード時は数分）が毎イテレーション発生するのが「進まない」の
正体だった。対策は3つ: (1) `dedupe_tool_results`（第0段階・LLM不使用）が
同一内容のツール結果の古い方を短い参照文に置換する（該当セッションでは
これだけで24168→14605トークン）。(2) ヒステリシス——発動は予算超過時のまま、
いったん発動したら目標（予算×`COMPACT_TARGET_RATIO`=0.6）まで一気に下げ、
再発までの間隔を空ける。切り詰め後も目標を上回る場合は予算以下でも要約に進む。
(3) 作業状態ダッシュボードに「同じツール呼び出し（同名+同引数）を3回連続で
繰り返している」警告を追加し、成功していても進展のない繰り返し（同じファイルの
再読など）をモデル自身に自覚させる。

**予算天井付近での空応答対策（先回り圧縮 + 強制圧縮）**: 実測で、書き込み系
ツールを一度も呼ばずドキュメント読解だけで会話が予算(24576トークン)の**99%**
（24240〜24316トークン）まで伸び、その状態でモデルが本文もツール呼び出しも
無い「空応答」を2回連続（自動リトライ1回を使い切っても）返して何も進まない
セッションが発生した。原因は二重: (a) 弱い/量子化されたローカルモデルは、
巨大な文脈を読み込んだ状態だと内部の思考でRESERVE_TOKENSの生成余白を
使い切り、可視の本文が一切残らないことがある（モデル側の限界）。(b) 従来の
空応答リトライは「続けてください」を1文足すだけで文脈量をほとんど減らさない
ため、ほぼ同じ状態でもう一度投げても同じ壁に当たるだけだった（LocalCoder側の
設計の隙間）。対策は2つ: (1) `compact_history`の発動判定を「予算超過時」から
「予算×`PROACTIVE_COMPACT_RATIO`(0.9)を超えた時点」に前倒しした——正式に超過
してから動くのでは手遅れで、天井の手前で先回りして縮める（該当セッションを
再生すると、実際に空応答が起きたメッセージより前の時点で新しいtriggerを
超えており、発動していれば空応答自体を防げていたことを確認済み）。
(2) 空応答で自動リトライする直前に`compact_history(..., force=True)`を呼び、
trigger未満でも強制的に圧縮してから次の試行に入るようにした（リトライは
文脈を実際に減らして初めて意味がある）。

**実行中バージョンの可視化**: 「バグを直してpushしたのに再現する」という報告を
調べたところ、原因はコードではなく**LocalCoderを再起動しておらず古いプロセスの
ままだった**ことだった（WSL側で`python3 server.py`が直した後もずっと動き続けて
いた）。同じ切り分けを毎回手作業でやらずに済むよう、起動時に`server.py`が
自分の置かれたディレクトリで`git rev-parse --short=7 HEAD`を実行し、結果を
`window.LC_VERSION`としてindex.htmlに埋め込みヘッダーに表示するようにした
（未コミットの変更があれば`+dirty`を付記）。`git log`の最新コミットハッシュと
画面表示を見比べるだけで、今動いているのが最新版かどうかが一目で分かる。
git実行に失敗した場合は"unknown"にフォールバックする。

**履歴タイトルが圧縮後にどれも同じになる問題**: 履歴一覧のタイトルは
`save_session`が「最初のuserメッセージの内容」から毎回作り直していたため、
圧縮が起きて`messages[0]`が「【自動要約】ここまでの会話が長くなったため、
古い部分は以下の要約に置き換えられた:...」という定型マーカーに置き換わった
瞬間、以後の保存すべてでタイトルもこの定型文になってしまい、履歴一覧で
別プロジェクトのセッションと区別がつかなくなっていた（実際にユーザーから
報告された）。対策として`derive_title`はマーカーを検出したら`_parse_marker`で
要約本文を取り出しそちらをタイトルにするよう変更し、さらに`save_session`は
**タイトルを初回保存時に一度だけ確定し、以後は既存タイトルを常に使い回す**
ようにした（毎回作り直すこと自体が原因なので、そもそも作り直さない）。既存の
壊れたタイトルを持つ履歴ファイル(5件)は、`derive_title`と同じロジックで
一度だけ書き換えるスクリプトを実行して移行した（`SUMMARY_BODY_SEP`導入前の
旧マーカー形式は区切りが無いため、`「...置き換えられた:\n」`の直後を本文と
みなすフォールバックが必要だった）。

**圧縮まわりの未着手アイディア（将来やるなら）**:
- **C. ユーザーの逐語指示の永続ピン留め**: 「覚えておいて」等のユーザー発言を
  検出し、圧縮ループの外側にある専用リストに保存して、system prompt直後に毎回
  そのまま挿入する。要約（LLM生成）を経由しないため、何度圧縮が起きても文言が
  変化・脱落しない。現状はSUMMARIZE_PROMPT側で「一字一句そのまま」と指示している
  だけで、遵守はLLM任せになっている。
- **D. 3層の段階的減衰**: 現状は「直近`KEEP_RECENT_MSGS`件=原文／それ以前=即要約」
  という二値のカットだが、中間層として「原文ではないが要約もしない、`_excerpt`
  抜粋のみ」の層を挟み、直近の詳細が急に消えるショックを緩和する。
- **E. 「現在のゴール」専用フィールド**: 要約プロンプトに現在の作業目標を1行で
  明示的に出力させ、圧縮結果の先頭に固定フォーマットで配置して本文中に埋没させない。
  次回以降の圧縮でもこのフィールドだけは常に無条件で引き継ぐ（Cと同様、圧縮の
  繰り返しで方向性が薄まるのを防ぐのが狙い）。

**空応答自動回復の設計ノート**: ローカルモデル（特にgpt-oss:20b）は、ビルド失敗など
行き詰まった状況で、本文なし・ツール呼び出しなしという「空応答」を返してターンを
静かに終えることがある。この場合サーバーは正常終了として扱うため、ユーザーには
何も表示されず、実質的に停止したように見えてしまう（実際に本番で遭遇し、
セッション履歴ファイルの最後のassistantメッセージがcontent=""であることで確認した
バグ）。対策として、`content`が空かつ`tool_calls`も無いターンを検出したら、
`EMPTY_RETRY_LIMIT`(1)回まで自動で「続けてください」という合成ユーザーメッセージ
（`EMPTY_RESPONSE_NUDGE`、`(システム自動継続)`と明記して会話履歴上も見分けられる
ようにする）を追加してループを継続する。それでも空応答なら諦めて
`{"type": "notice", "message": "⚠ ..."}` をGUIに送りユーザーに手動介入を促す
（`server.py`参照）。モックテストで両ケース（1回の自動継続で回復／2回とも空で
警告して停止）を検証済み。無限ループの心配はない: `empty_retries`は
`handle_chat`のローカル変数なのでユーザーの1メッセージごとにリセットされ、
かつ`EMPTY_RETRY_LIMIT`で上限を切っている。

**HTTP再試行の設計ノート**: 上記の「空応答」とは別に、Ollama呼び出し自体が
`urllib.error.URLError`（HTTP 500等、接続断も含む）で失敗するケースがある
（実際にgpt-oss:20bで一過性の500エラーに遭遇した）。`for chunk in
ollama_stream(payload):` を`try/except urllib.error.URLError`で囲み、
`HTTP_RETRY_LIMIT`(1)回まで`HTTP_RETRY_DELAY`(2秒)待って自動再試行する
（GUIに🔔で通知）。上限を超えたら`raise`で外側のハンドラに委譲し、従来通り
赤いエラー表示で停止する。こちらも`http_retries`は`handle_chat`のローカル
変数でユーザー1メッセージごとにリセットされ、再試行は`messages`を一切
変更せずに行う（失敗時点では`assistant`メッセージがまだ履歴に追加されて
いないため、同じ`payload`でそのまま再試行しても履歴に矛盾は生じない）。
モックテストで回復ケース（1回失敗→再試行で成功）と行き詰まりケース（2回とも
失敗→通知後に通常のエラー表示で停止、呼び出しは2回で打ち切り）の両方を
検証済み。

**完了ノーティスの設計ノート**: ユーザーがタスクマネージャーでGPU/CPU監視をしながら
待つなど、画面から目を離している間に処理が終わっても気付けない、という要望への
対応。`index.html`側で`/api/chat`のストリーム読み取りループが終わった直後
（成功・エラー・停止いずれの経路でも通る`send()`関数の共通末尾）に`notifyDone()`
を呼ぶ。中身は2つ: (1) Web Audio APIで生成する短いベル音（外部音声ファイル不要、
`OscillatorNode`+`GainNode`で880Hzのサイン波を0.4秒フェードアウト）、(2) ブラウザ標準
の`Notification` API によるデスクトップ通知（ページ読み込み時に`Notification.
requestPermission()`を1回呼んでおく）。デスクトップ通知はウィンドウがフォーカスを
失っていても表示されるため、まさに「他の作業をしながら気付きたい」という要望に合う。
ユーザー自身が「■停止」を押した場合は`stoppedByUser`フラグで通知を抑制する
（自分で止めた操作についてわざわざ知らせる必要はないため）。サーバー側の変更は
不要（完全にクライアント側`index.html`のみで完結する機能）。

**作業時刻の記録の設計ノート**: 「プロンプトを投げた時」と「作業が終わった/中断した
時」を記録してほしいという要望への対応。`handle_chat()`冒頭で
`turn_started_at = time.time()`を取り、`turn_status`（初期値`"completed"`）を
以降の各終了経路で上書きする: 停止ボタン検知2箇所（ストリーム受信中・ツール実行中）
で`"stopped"`、`for...else`のMAX_ITER到達で`"max_iter"`、
`BrokenPipeError`/`ConnectionResetError`で`"disconnected"`、`URLError`や
その他の例外で`"error"`。`finally`節で`{"started_at", "ended_at", "status"}`の
`turn`辞書を組み立て、`save_session(..., turn=turn)`に渡す。

`save_session()`は元々毎回ファイル全体を上書きする実装だったため、単純に
`turns`キーを追加しただけでは前回までの記録が消えてしまう。そのため保存前に
既存ファイルがあれば`turns`配列を読み出し、そこに今回の`turn`をappendしてから
書き戻すようにした（`server.py`参照）。個々のメッセージ内容（`messages`配列）には
一切手を入れていない——時刻は独立した`turns`配列にのみ記録することで、
Ollamaに送るメッセージのスキーマや`compact_history()`のトークン見積もりに
影響を与えないようにしている。

検証: (1) 単体テストで`save_session`を複数回呼び、`turns`が上書きされず
蓄積されること、(2) 実際のgpt-oss:20bでの正常完了リクエストで
`status="completed"`・実測所要時間（37.3秒）が正しく記録されること、
(3) 実際に停止ボタンを押した場合に`status="stopped"`となること、
(4) ollama_streamを常時失敗にモックした場合に`status="error"`となること、
の4パターンを確認済み。ワークスペース検証エラーなど、`try/finally`ブロックに
入る前に早期returnするケースは対象外（実質的な作業が何も始まっていないため）。

**システムプロンプト規律強化の設計ノート**: 実際のPicoCalcプロジェクトでの運用中、
ローカルモデル（gpt-oss:20b）に次の失敗パターンが繰り返し観測された:
(a) ユーザーが仕様書等で正確な値（GPIOピン番号・I2Cアドレス等）を与えても、
それを使わず別の"それらしい"値を捏造する、(b) `pico_fat_fs`のような実在しない
CMakeターゲット/関数名を存在を確認せず使う、(c) ビルド検証を自分で実行せず
「次はcmake/makeを実行してください」とユーザーに丸投げして終わる。
`SYSTEM_PROMPT`にこれらへの対抗ルールを追記した:
「与えられた正確な値はそのまま使い、自分の推測に置き換えない」
「ライブラリ/関数/パスの実在を確信できなければ使う前に確認する」
「run_commandで実際に実行し、成功を観測するまでタスク完了にしない。
『次にユーザーが○○を実行してください』で終わらせない」
「以前ビルド/実行に成功していたファイルを編集したら、壊していないか再確認する」。

検証（モックではなく実際のgpt-oss:20bで実施）:
1. 正確な値（I2C1/GPIO6/GPIO7/0x1F等）をそのまま転記させる単純タスク → 全項目一致
2. 仕様書を読んでコード（`kbd_init()`関数）に落とし込ませるタスク → i2c1・
   GPIO6/7・0x1Fを正しく使用（以前のようにi2c0・GPIO4/5・0x20への捏造なし）
3. わざと出力ファイル名が壊れたMakefileを渡し「makeを実行して確認して」と
   依頼 → 自力でMakefileの誤りを発見・修正し、`make`実行→生成物を`list_dir`で
   確認→実行して終了コードまで確認してから完了報告（「次はユーザーが」という
   丸投げが発生しなかった）

**Windows操作対応の設計ノート**: LocalCoderはWSL(Linux)内で動くが、WSLの
相互運用機能により**元々**Windowsコマンドは実行できた——`run_command`は
サンドボックス無しの`bash -lc`なので、`powershell.exe -Command "..."`や
`.exe`直接呼び出しでWindows側が動く（`powershell.exe`が実際に動作することを
確認済み）。Windowsファイルも`/mnt/c/...`から読める。ブロックされていたのは
2点だけ: (1) 専用ファイルツール(read_file/write_file/edit_file/list_dir)と
作業フォルダが`$HOME`配下に制限されていたため`/mnt/c`のWindowsファイルを直接
編集できない、(2) システムプロンプトがWindows操作の存在をモデルに伝えていない。
対応として、`under_home()`を`ALLOWED_ROOTS`(既定=`[$HOME, /mnt]`、環境変数
`LOCALCODER_ALLOWED_ROOTS`で上書き可)に対する`under_allowed()`へ一般化し、
`handle_chat`のワークスペース検証と`list_subdirs`(フォルダ選択ダイアログ)を
これに切り替えた。`resolve_path`(ファイルツールのサンドボックス)は元々
「選ばれたワークスペース配下」に閉じる実装なので変更不要——ワークスペースが
`/mnt/c/...`になれば自動的にそのWindowsフォルダ内に閉じる。システムプロンプトには
「Windowsファイルは`/mnt/c`配下」「Windowsコマンドは`powershell.exe`で(cmd.exeは
cwdがWSLパスだと警告を出すのでpowershell.exe推奨)」を明記した。

セキュリティ上の注意: 「ワークスペースを`$HOME`配下に制限」は元々4層防御の
1層だったが、`run_command`は元からサンドボックス無しでこの層の対象外だった
（つまりこの層は専用ファイルツールにしか効いていなかった）。`/mnt`まで許可すると
専用ファイルツールもWindowsドライブに届くようになる。外部Webページからの悪用を
防ぐ本丸(トークン+localhostバインド)は不変。`$HOME`だけに戻したい場合は
`LOCALCODER_ALLOWED_ROOTS=$HOME`を設定する。

検証: 単体テスト13件(HOME/`/mnt`配下は許可、`/etc`・`/usr`・`/`は不許可、
Windowsディレクトリの一覧取得、`/mnt/c/Users`の親が`/mnt/c`で辿れること、
`/mnt`はルートなので「上へ」なし、範囲外はHOMEにフォールバック、resolve_pathが
Windowsワークスペース内は許可し外への脱出は拒否)、および実際のgpt-oss:20bで
`/mnt/c/Users/fyone/lc_win_test`を作業フォルダにしてwrite_fileでWindows実ファイル
(`C:\Users\fyone\lc_win_test\note.txt`)を作成し、`powershell.exe`でWindowsユーザー名
(`fyone`)を取得するE2Eが成功することを確認済み。

**作業状態ダッシュボードの設計ノート**: システムプロンプトの規律強化だけでは、
「さっき変更したファイルを忘れる」「同じ失敗を繰り返す」という、会話ログの
自然文からは拾いにくい種類の失敗までは防げない。要約(`compact_history`/
`summarize_old`)はLLMに頼る以上、要約自体が雑になるリスクを抱えたままである。
そこで会話ログの圧縮とは別に、**機械的に(LLMを使わず)確定できる事実だけ**を
毎回組み立てて画面外のダッシュボードとして注入する方式を追加した:

- `_iter_tool_calls_with_results()`: `assistant`の`tool_calls`と、直後に続く
  同じ順序の`tool`メッセージ群を突き合わせ、(ツール名, 引数, 結果) を発生順に
  取り出す
- `build_work_state()`: 上記から「変更したファイル一覧」（`write_file`/
  `edit_file`の対象パス）、「直近`RECENT_COMMANDS_SHOWN`件のコマンドと
  exit_code」、「同一コマンドが`FAIL_REPEAT_THRESHOLD`(3)回連続で失敗したら
  警告」を組み立てる（`server.py`参照）

この結果は`handle_chat`の`for it`ループ毎に再計算し、**保存される会話履歴
(`messages`)には一切追加しない**——Ollama呼び出し1回分だけの使い捨てメッセージ
（`call_messages = messages + [{"role":"user","content":WORK_STATE_PREFIX+...}]`）
として差し込む。要約プロンプトのような意味的な判断（目的・サブタスク・
どの仮説が外れたか）はLLMに頼らざるを得ないため今回は対象外とし、まずは
コストゼロ・100%正確な機械的部分だけを実装した（ユーザーとの設計議論の結論）。

なお圧縮済み(要約に置き換わった)古い部分は`tool_calls`構造が失われるため、
このダッシュボードの「変更ファイル一覧」は直近の非圧縮ウィンドウのみを反映する
（古い変更点は要約の自然文側に残る）。

検証: (1) 単体テスト10件（空履歴/ファイル抽出/コマンド結果表示/3回連続失敗の
検知/2回では未検知/別コマンド成功後は誤検知しない/`messages`本体を書き換えない
こと、を確認）、(2) 実際のgpt-oss:20bでhello.py作成タスクが従来通り動くこと、
(3) わざと`cmake ..`を3回連続失敗させた履歴から続行させた場合、4回目も
同じコマンドを盲目的に繰り返さず、`list_dir`/`find`で状況を調べ直してから
別のアプローチ（`cmake -S . -B build`）に切り替えたことを確認済み
（ただしこのテストはワークスペースが実際に空だったため、その事実自体が
調査を促した可能性もあり、ダッシュボード警告単体の効果を完全には
切り分けられていない）。

**フォルダ選択ダイアログの設計ノート**: ブラウザ標準の`<input type="file" webkitdirectory>`は
セキュリティ上の理由で絶対パスを返さない（相対パスのファイル一覧しか取れない）ため、
作業フォルダ選択には使えない。代わりにサーバー側に`GET /api/browse?path=...`
（要トークン、`$HOME`配下のみ許可・範囲外は`$HOME`にフォールバック、隠しディレクトリは
除外）を追加し、GUI側にモーダル式のディレクトリブラウザを実装した
（`list_subdirs()`, `server.py`参照）。「📁 参照」ボタン→現在の作業フォルダ欄の値を
起点に一覧表示→ディレクトリクリックで下降、「.. (上へ)」でHOMEまで上昇可、
「このフォルダを選択」で確定、という単純なナビゲーション。

**edit_fileの設計ノート**: 完全一致のfind/replace（old_string→new_string、
`replace_all`オプション付き）。既存ファイルの部分修正で全文書き換え（write_file）を
使うと、大きいファイルほど出力トークンを浪費し、小型モデルは途中の行を書き換え忘れて
ファイルを壊しやすい。そのためシステムプロンプトで「部分修正はedit_file優先、
write_fileは新規作成か全面書き換えのみ」と誘導している。小型モデルは完全一致の
old_stringを作るのが苦手な場合があるため、失敗時のエラーメッセージに次の一手
（read_fileで正確にコピーせよ／一意になるまで文脈を足せ／諦めてwrite_fileにせよ）を
書いてあり、モデルが自己回復できる。gpt-oss:20bでは「PORT変更とDEBUG変更を
edit_file 2回でピンポイント修正→read_fileで検証」という理想的な挙動を確認済み。

**POST APIはCSRFトークン必須**: 起動ごとに生成するランダムトークンを`index.html`配信時に
埋め込み、全POSTで `X-LocalCoder-Token` ヘッダ＋Origin/Host/Content-Type検証を行う
（127.0.0.1バインドだけでは、同一PC上の悪意あるWebページからのno-cors POSTを防げないため）。
**全GETもHostヘッダを検証**（DNSリバインディング対策）し、履歴を返す`/api/sessions`・
`/api/session?sid=`はGETでもトークン必須（`/`・`/api/health`・`/api/models`は
トークン取得前に呼ぶ必要があるためチェック対象外。機密を返さないので問題ない）。

**依存JSはCDNではなく同梱**: `marked`・`DOMPurify`は`~/localcoder/vendor/`にバージョン
固定で配置し、`server.py`が`/vendor/*.js`として静的配信する。このページには
`window.LC_TOKEN`（コマンド実行に到達できる権限）が埋め込まれているため、CDN配信の
JSを使うとCDN側の改ざんやバージョン無指定URLの自動更新がそのままLocalCoderの実行権限
になってしまう。同梱により完全オフラインでも動作する。

**web_searchの実装ノート**: `https://html.duckduckgo.com/html/?q=…` をUser-Agent偽装付きで
GET し、`result__a`（タイトル+URL）と `result__snippet` を正規表現で抽出。結果URLは
`uddg=` パラメータに包まれているのでURLデコードして取り出す。一部サイト（raspberrypi.com等）は
fetch_url を403で拒否するが、エラーはそのままモデルに返るので別の検索結果を自分で試す。

**SSEイベントプロトコル** (`data: {json}\n\n` 形式):
`think`(思考トークン) / `token`(本文トークン) / `turn_done`(1応答完了) /
`tool_start`,`tool_end`(ツール実行) / `history`(全会話履歴→クライアントが保持し次回送信) /
`all_done` / `error`

**会話状態はクライアント側が保持**（サーバーはステートレス）。`history` イベントで
tool呼び出し含む完全履歴を返し、次のPOSTでそのまま送り返す方式。

**チャット履歴の永続化**: 各会話は完了時（エラー・停止時も含む）に
`~/localcoder/history/<sid>.json` へ自動保存される（title=最初のユーザー発言、
updated_at、model、workspace、全messages）。GUIの左サイドバーが `/api/sessions` で
一覧表示し、クリックで `/api/session?sid=` から読み込んで会話を再開できる
（tool呼び出しカードも復元）。sidはファイル名になるため `[A-Za-z0-9_-]` のみに
サニタイズすること（パストラバーサル防止）。履歴はプライベートな内容を含むので
`.gitignore` に `history/` を入れる。

**画像・PDF対応の設計ノート**: 「画像やPDFを扱えるようにしてほしい」という要望への対応。
検討した2案:
(a) チャット欄に添付UI(📎ボタン/ファイル選択)を追加し、クライアント側でbase64化して
送る方式、(b) 既存のエージェント自律ループに乗せ、専用ツールとして実装する方式。
LocalCoderは元々「エージェントがワークスペース配下(Windowsドライブ含む)のファイルに
自由にアクセスできる」設計であり、ユーザーは既に`read_file`にパスを渡すだけでよい
仕組みに慣れている。(a)は添付UI・capability連動の有効/無効化・base64送受信など
UIの複雑さが増す一方、(b)は既存のtool-callingループにそのまま乗るため実装量が
少なく、一貫性も高い。ユーザーとの設計議論を経ず(b)を選択したが、既存アーキテクチャ
との親和性を優先した判断であり、「チャットに直接ファイルを貼り付けたい」ニーズが
出た場合は(a)を後から追加できる(両立可能)。

- `read_file`が`.pdf`拡張子を検出したら`pdftotext -layout <path> -`をsubprocessで
  呼び出しテキスト化する(`pdf_to_text()`)。`pdftotext`(poppler-utils)が無い環境では
  `FileNotFoundError`を捕まえ導入コマンドを案内するエラー文字列を返す(サーバー全体は
  落とさない)。スキャン画像PDF等で抽出結果が空文字なら、その旨をエージェントに返す。
- 新規ツール`view_image`: パスを受け取り拡張子(`IMAGE_EXTS`)を検証、
  `model_capabilities(model)`(`/api/show`の`capabilities`配列をモデル名でキャッシュする
  `_CAPS_CACHE`辞書経由、`vision`を含むか判定)で現在のモデルがvision対応か確認する。
  非対応ならエラー文字列を返すのみ(ここでOllamaへの実際の画像送信は行わない)。
  対応していれば画像をbase64化して`pending_images`リスト(呼び出し元から渡される
  可変リスト)に積み、`"OK: loaded ..."`という短いテキストだけをtool結果として返す。
- `handle_chat`のツール実行ループが1イテレーション分の`tool_calls`を処理し終えた後、
  `pending_images`が空でなければ、`{"role":"user","content":"(view_imageで読み込んだ
  画像)","images":[...]}`という合成のuserメッセージを`messages`に追加してから次の
  Ollama呼び出しへ進む。**tool役割のメッセージに直接`images`を載せる案は採らなかった**
  ——Ollamaのvisionテンプレートはuser/assistantロールでの画像を前提にしている実装が
  大半で、tool役割での画像添付が全モデルで確実に効くかを検証していないため、より
  安全側のuserメッセージ合成を選んだ。ライブ表示用に`{"type":"image","b64":...}`の
  SSEイベントも同時に送る(この合成メッセージは通常の会話履歴として保存されるため、
  セッションを開き直した際もそのまま画像として復元される)。
- `/api/models`のレスポンスを`["name", ...]`から`[{"name":..., "vision":bool}, ...]`
  に変更し(`index.html`の`loadModels()`も追従)、モデル選択の横に
  「👁 画像対応 / 🚫 画像非対応」バッジを常時表示するようにした。`model_capabilities()`
  の結果はモデル名ごとにプロセス内キャッシュするため、モデル一覧を開くたびに毎回
  `/api/show`を叩き直すことはない(モデルの追加/削除はollama pull/rmでしか起きず、
  サーバー起動中に変わることは事実上ない)。
- トークン見積もり(`estimate_tokens`)に`TOKENS_PER_IMAGE`(768、目安値)×画像枚数を
  加算するようにした。画像はテキストではないため元の文字数ベースの概算では
  ほぼゼロと見積もられてしまい、コンテキスト溢れの原因になり得るため。
- 要約(`render_transcript`、`compact_history`の入力)では画像の中身(base64)を
  要約対象に含めず、「[画像N枚が添付されていた(要約には含まれない)]」という
  短いマーカーに留める。base64をそのまま要約プロンプトに混ぜるとトークンを
  無駄に消費し、要約自体が壊れるリスクがあるため。

検証: 実際に動かしているOllama(qwen3-vl:8b/gpt-oss:20b/qwen3:8b等)に対し、
(1) 自作の最小PDF(`pdftotext`が標準搭載のPythonライブラリで生成困難なため生の
PDF構文で自作)を`read_file`で読ませてテキストが一致すること、(2) 単色PNGを
`view_image`で読ませ、vision対応のqwen3-vl:8bが正しく色を回答すること、
(3) vision非対応のqwen3:8bで`view_image`を呼ぶと明示的なエラーが返り、
エージェントがユーザーへのモデル切り替え案内を自分の言葉で生成すること、
(4) 保存済みセッションJSONに`images`フィールドが正しく残り、履歴を開き直すと
ブラウザ側でサムネイル(`<img>`)として実際に描画される(`naturalWidth/Height`が
元画像と一致)こと、の4パターンをE2Eで確認済み。

**ターン診断情報とschema_versionの設計ノート**（`IMPROVEMENTS.md` §2.3/§8.3の実装）:
障害調査のたび、履歴JSONを手作業で読み込んでトークン見積もり・ツール呼び出し数・
空応答回数などをその場でPythonスクリプトを書いて計算していた。この手間をなくすため、
`handle_chat`の各ターンで次を機械的に記録し`turn`辞書に含めるようにした:
`est_tokens_start`/`est_tokens_end`（開始時・終了時点の推定トークン数）、
`compact_count`（圧縮が実際に発動した回数。`compact_history`の戻り値が入力と
同一オブジェクトかどうかで判定——no-opならオブジェクトを変更せず返す既存の
実装をそのまま利用できた）、`tool_call_count`/`tool_exec_seconds`（ツール
呼び出し回数と合計実行時間）、`iterations_used`（`MAX_ITER`のうち実際に
使った回数）。あわせて履歴JSONに`schema_version`（現在2）を導入した。
形式を変える機会は今後もあるはずで、変更の起点になったこの回でバージョン
番号を導入しておく。診断フィールドの収集ロジック自体はHTTPハンドラ内の
状態を跨ぐため単体テストの対象外（`server.py`の段階的分割で`handle_chat`を
分離すればテスト可能になる。`IMPROVEMENTS.md` §8.1参照）だが、`save_session`
が任意の`turn`辞書をそのまま保存・往復できることは`tests/test_history_io.py`
で確認している。実チャットでの動作確認も実施済み（`schema_version: 2`と
診断フィールドが実際に書き込まれることを確認）。

**コマンド出力の全文保存の設計ノート**（`IMPROVEMENTS.md` §4.2の一部実装。
§4.1の構造化ツール結果は今回は見送った——既存の`ERROR:`/`OK:`/`exit_code=0`
という文字列規約が`build_work_state`/`extract_changed_files`/
`track_tool_repeat`等の広範囲に定着しテスト済みでもあるため、書き換えの
リスクに見合う効果が今は無いと判断した）。`run_command`は出力が12KBを
超えるとモデル向けに先頭6KB+末尾6KBへ切り詰めていたが、切り詰め後の文字列が
そのまま会話履歴にも保存されるため、中間にビルドエラー等の重要な情報があると
永久に失われていた。`save_full_tool_output`が、切り詰めが発生した場合のみ
（＝出力が小さければ何もしない）完全な出力を
`HISTORY_DIR/tool_output/<sid>/<call_id>.txt`へ保存するようにした
(`HISTORY_DIR`配下なので`.gitignore`の`history/`で自動的に除外される)。
`sid`/`call_id`は`exec_tool`まで新たに引き回す必要があった
(`tc.get("id")`をツール呼び出しIDとして利用)。`call_id`はファイル名に使う
ため`_safe_sid`と同様のホワイトリスト方式でサニタイズしている。単体テスト
に加え、実チャット（`gpt-oss:20b`で20KB出力のコマンドを実行）でも
`tool_output/<sid>/<call_id>.txt`に全文(20001バイト)が保存され、会話履歴側は
従来通り切り詰められる(12031バイト、`...[truncated]...`含む)ことを確認した。

**作業サマリーUIの設計ノート**（`IMPROVEMENTS.md` §7.1の実装）: ターン診断情報
(§2.3)を保存するだけではUIから見えず、結局履歴JSONを直接読む必要があった。
`handle_chat`が正常終了経路(completed/max_iter/stuck)の末尾で新しいSSEイベント
`{"type":"summary","data":{...}}`を送るようにし(`stopped`/エラー/切断時は
SSEが既に途切れている可能性が高いため送らない)、フロント側`summaryCard()`が
「終了理由・所要時間・ツール呼び出し回数・変更ファイル数・圧縮回数」を
チャット末尾にカードで表示する。変更ファイルの一覧(パス)はカードの`title`
属性(ホバーで確認可能)に入れている。

履歴を開き直した時（`openSession`）にも直近ターンのサマリーを表示したかったが、
`turn`辞書には`changed_files`の一覧までは保存していない(件数の`tool_call_count`
等はあるが)。差分は次の1点のみ: ライブ実行中はファイルパスの配列(ホバーで一覧
表示可)、履歴再表示時は保存された`changed_files_count`(件数のみ、ホバー無し)。
`summaryCard()`は`changed_files`が配列でも数値でも動くようにして共用した
(`Array.isArray`で分岐)。この非対称性が気になるなら、`turn`辞書に
`changed_files`のパス一覧そのものを保存する方向で拡張できる。

診断情報が無い古い履歴ファイル(このセッション以前に作成されたもの)を開くと、
`tool_call_count`等が`undefined`になるため`||0`でフォールバックし、0件表示に
なる(エラーにはならない)。実ブラウザ(Chrome, headless)で確認: (1)
ライブチャットでサマリーカードが正しい値(終了理由=完了、所要時間、ツール
呼び出し0回)で表示されること、(2) 診断情報を持たない旧履歴を開いても
サマリーカードが(0埋めで)クラッシュせず表示されること、の両方を確認済み。

**起動時セルフチェックの設計ノート**（`IMPROVEMENTS.md` §9.2の実装）: 「直した
はずなのに直っていない」の原因が実は再起動忘れだった、Ollamaが落ちているのに
気づかず作業を始めてしまう、といった事故が実際にあった。`run_self_check`が
起動時に1回だけ (1) Ollama接続可否と推奨モデル(`gpt-oss`/`ornith`系統)の有無、
(2) `pdftotext`(poppler-utils)の有無、(3) `ALLOWED_ROOTS`の各パスが実在するか、
(4) `HISTORY_DIR`に書き込めるか、を確認し結果を`SELF_CHECK_RESULTS`に保持する
(モジュールグローバル。`main()`で`ThreadingHTTPServer`のbindに成功した後に
一度だけ実行し、コンソールにも`[OK]`/`[NG]`で出力する)。**全項目が警告に
留まり、失敗してもサーバー起動は止めない**——「依存機能の段階的劣化」
(§9.4)の既存方針に合わせている(Ollamaが後から起動する運用や、pdftotext無しで
PDF以外の機能を使い続けることを許容する)。フロントは新設の`GET /api/selfcheck`
(トークン不要、`/api/health`と同様に機密性が低いため)をページ読み込み時に
1回叩き、失敗項目があれば`#selfCheckBanner`に要約を表示、クリックで詳細
(`detail`文字列)に展開する。全項目成功なら何も表示しない(バナーで画面を
埋めない)。ポート使用中チェックと(将来の)WSL/Windows相互運用チェックは
今回は含めていない——前者は`main()`の既存の`bind`失敗処理で実質検出済み、
後者は判定方法の設計が固まっていないため見送った。単体テストは
`urllib.request.urlopen`をモックして5項目それぞれの成功/失敗を検証。実ブラウザで
両方確認済み: (1) 正常時(Ollama到達可)はバナー非表示、(2)
`LOCALCODER_OLLAMA`を疎通不可能なアドレスに設定して起動すると、バナーに
「⚠ 起動時チェックで2件の問題」が表示され、クリックで具体的なエラー内容
(`URLError: <urlopen error timed out>`)まで展開されること。

**ユーザー指示の逐語ピン留めの設計ノート**（`IMPROVEMENTS.md` §3.2の実装、
REBUILD.mdの旧「未着手アイディアC」）: `SUMMARIZE_PROMPT`は要約時に「覚えて
おいてと言われた事項は一字一句そのまま」と指示しているが、これはLLMが従う
という前提に立った緩い保証でしかなかった。変更ファイル一覧(§2の設計ノート)で
使った「機械抽出→専用ブロックで要約対象外にする」パターンをそのまま
ユーザー発言にも適用した。`extract_pinned_instructions`が`role=="user"`の
発言から`PIN_TRIGGER_RE`(「覚えて」「忘れないで」)にマッチするものを機械的に
(LLM不使用・発生順・重複除去で)集める。`_parse_marker`/`build_marker`は
3値タプル`(summary, files, pinned)`を返すように拡張し、マーカー内に
`PINNED_SECTION_HEADER`ブロックを追加した(順序: 要約本文→ファイル一覧→
固定指示。`_parse_marker`は末尾のPINNED_SECTION_HEADERから先に`partition`し、
残りから従来通りFILES_SECTION_HEADERを`partition`するため、ファイル一覧側の
既存パース処理には影響しない)。`compact_history`は圧縮のたびに
`prev_pinned + 新規分`をマージして引き継ぐ(ファイル一覧と全く同じ合流方式)。
あわせて`build_work_state`にも「ユーザーの固定指示(厳守):」行を追加し、
圧縮されていない直近ウィンドウでも毎ターン思い出させるようにした。

トリガー語を「覚えて」「忘れないで」の2語に絞った理由: 既存の
`SUMMARIZE_PROMPT`が最初から「覚えておいて」を基準にしており、広すぎる
キーワード(例: 「〜してください」等)は日常的な指示文の大半にマッチして
ノイズになる。誤検出を避け、ユーザーが明示的に「これは覚えておいてほしい」と
示した発言だけを拾う設計にした。「現在のゴール専用フィールド」(§3.1、旧
アイディアE)は今回のスコープに含めていない——ゴールはユーザー発言から機械的に
抽出できず、抽出にLLMを使うなら新たな要約ステップが増え、今回の「機械的に
確実」という設計原則から外れるため、着手する場合は別途設計が必要。

単体テストは`_parse_marker`/`build_marker`のラウンドトリップ(固定指示あり/なし/
ファイル一覧との併存)、`extract_pinned_instructions`の各トリガー、
`compact_history`が2回の圧縮を跨いで固定指示を保持すること、
`build_work_state`の表示/非表示、を検証。実チャットでもトリガー語を含む
依頼が正常に処理されることを確認済み(サーバーが強制終了・クラッシュしない
ことのみ確認——圧縮を跨ぐ実地検証は単体テストのFakeOllamaによる再現で担保)。

**`IMPROVEMENTS.md`の実装順序見直し(外部MCP構想・メタ認知再評価パスとの統合、
2026-07-14)**: 開発者がGoogle Drive上に作成した「PicoCalc開発支援エキスパートMCP」
設計文書を読んだところ、その文書自身が「LocalCoderへの導入タイミング」として、
テスト基盤・現在ゴールと継続指示の独立保持・完了条件の機械的管理・構造化ツール
結果・ツール実行部分の分離・ToolProvider相当の共通インターフェース、をMCP接続の
前提として明示していた。あわせてpullした`METACOGNITIVE_REPLANNING.md`の方針
再評価パスも、再評価コンテキストとして「完了条件の達成状況」を使う設計になって
おり、この2つの新しい構想が独立に同じ前提（現在のゴール専用フィールド§3.1、
完了条件の機械的管理§3.3）を要求していることが分かった。`IMPROVEMENTS.md`に
これを反映し、(1) 各項目に実装済み/未着手のマークを付けて現状を可視化、
(2) §3.1・§3.3・§4.1(構造化ツール結果)・§8.1(ツール実行の分離)を「MCP対応の
前提」として第3.5段階にまとめて優先度を明示、(3) 新規§13として、外部文書との
対応表・`ToolProvider`インターフェース案(組み込みツールとMCPツールを同じ
`exec_tool`ディスパッチへ統合する薄い抽象化)・役割分担・未確定事項を追記した。
コード変更は無く、ロードマップ文書の更新のみ。

**完了条件の機械的管理の設計ノート**（`IMPROVEMENTS.md` §3.3の実装、
第3.5段階=MCP対応前提の1番目）: モデルが実際にはビルド/テストで確認していない
のに「完了しました」と申告する事故を防ぐため、`find_unverified_changes`が
`write_file`/`edit_file`で変更した(かつ結果が`ERROR`で始まらない=成功した)
ファイルのうち、その後に一度も`run_command`が呼ばれていないものを機械的に
検出する(発生順・重複除去、LLM不使用)。`run_command`が1回でも呼ばれれば
それ以前の変更は「検証試行済み」とみなしてリストをリセットする——ここで
判定したいのは「検証を試みたか」であり「検証に成功したか」ではない
(成否は既存の直近コマンド結果の表示で別途分かる)。`extract_changed_files`
と全く同じ`_iter_tool_calls_with_results`ベースの実装パターンを踏襲した。

`build_work_state`に「⚠ 未検証の変更(ビルド/テストを一度も実行していない): …
これらを検証するまで作業を完了したと報告しないこと。」という行を追加し、
毎ターンのダッシュボードで直接モデルに伝えるようにした。あわせて
ターン診断情報(`turn`辞書)に`unverified_changes_count`を、作業サマリーSSE
イベント(`{"type":"summary"}`)に`unverified_changes`(ファイルパスの配列)を
追加し、フロント側`summaryCard()`にも「⚠ 未検証: Nファイル」を表示するように
した(未検証がある場合は`status-stuck`と同じ警告色を流用)。

`extract_changed_files`と同様、圧縮済みの古い部分は対象外——直近の非圧縮
ウィンドウのみを反映する(圧縮を跨いだ未検証追跡は今回のスコープに含めていない。
必要になれば`changed_files`と同じマーカー方式で拡張できる)。

単体テストは`find_unverified_changes`(検証コマンド前後の切り分け・失敗した
書き込みの除外・重複除去)と`build_work_state`の表示/非表示を検証。実チャットで
`write_file`のみを呼び`run_command`を呼ばないよう指示し、`turn`に
`unverified_changes_count: 1`が記録されること、作業サマリーカードに
「⚠ 未検証: 1ファイル」が表示されツールチップにファイルパスが出ること
(履歴再表示時は件数のみでツールチップ無し、ライブ実行時は配列でツールチップ
あり、という既存の`changed_files`と同じ非対称性)を確認した。

**現在のゴール専用フィールドの設計ノート**（`IMPROVEMENTS.md` §3.1の実装、
第3.5段階=MCP対応前提の2番目）: ゴールはユーザー発言から機械的に抽出できない
（今日先に書いた設計ノートで見送った理由そのもの）。今回は、追加のLLM呼び出しを
増やさずに解決するため、既存の要約プロンプト（`SUMMARIZE_PROMPT`/
`UPDATE_SUMMARIZE_PROMPT`、圧縮のたびにどのみち1回呼ばれる）に相乗りする形にした。
出力の1行目を必ず`GOAL: <現在の最終目標を1文で>`という固定形式にするよう指示し、
`_extract_goal_line`が正規表現で先頭のGOAL行を抽出して要約本文から取り除く。
抽出したゴールは`GOAL_SECTION_HEADER`ブロックとしてマーカーの末尾（ファイル一覧・
固定指示より後）に付記し、`_parse_marker`は末尾から順に(ゴール→固定指示→
ファイル一覧→要約本文)とpartitionしていく。

`update_summary`/`summarize_old`は`(要約本文, ゴールまたはNone)`のタプルを返す
ように変更した(チャンク分割される場合は最後のチャンクの判定を採用)。
`compact_history`は新しいゴール判定が得られなければ(GOAL行を出力しなかった等)
前回のゴールをそのまま引き継ぐ——ファイル一覧・固定指示と同じ「機械的に確実な
引き継ぎ」の考え方だが、ゴール自体はLLM判定なので完全な保証ではない。

開発時に**実際にバグを1件発見した**: `GOAL_LINE_RE`を最初`r"^GOAL:\s*(.*)$"`
(MULTILINE)で書いたところ、`\s`は改行にもマッチするため、GOAL値が空(または
末尾が空白+改行)の場合に次の行の内容まで巻き込んで抽出してしまっていた。
`[ \t]*`に修正し、単体テスト`test_empty_goal_value_is_none`で検知・固定した。

`build_work_state`に「現在のゴール: …」行を追加(最優先で先頭に表示)。ただし
これは圧縮マーカーが存在する場合のみ(＝一度も圧縮が起きていない短い会話では
表示しない——その場合はユーザーの元の依頼がまだ会話内に直接残っているため
不要、`extract_current_goal`参照)。

単体テストは`_extract_goal_line`(抽出・GOAL行無し・空値のバグ回帰)、
`extract_current_goal`(マーカーからの抽出・圧縮前はNone)、`compact_history`が
2回の圧縮を跨いでゴールを保持すること(新規分にGOAL行が無くても前回のゴールを
維持)、`build_work_state`の表示/非表示を検証。実際に`gpt-oss:20b`と
`qwen2.5-coder:7b`の両方へ`summarize_old`を直接呼び出し、どちらも指示通り
`GOAL: ...`形式の1行目を出力して正しく抽出できることを確認した。

**ツール実行部分の分離＋ToolProviderインターフェースの設計ノート**
（`IMPROVEMENTS.md` §8.1の一部＋§13.2の実装、第3.5段階=MCP対応前提の3番目）:
従来の`exec_tool`は組み込みツールの分岐(`if name == "run_command": ...`の連鎖)を
そのまま抱えていた。これを`BuiltinToolProvider`クラスへ移し、
`ToolProvider`（`typing.Protocol`）という共通インターフェース
(`list_tools() -> list[dict]`、`call_tool(name, args, ctx) -> str`)を定義した。
ツール実行に必要な状態(`ws`/`cancel`/`model`/`pending_images`/`sid`/`call_id`)は
バラバラな引数ではなく`ToolContext`という1つのオブジェクトにまとめて渡す。
`TOOL_PROVIDERS`（現状は`BuiltinToolProvider`のみのリスト）と、ツール名から
プロバイダを探す`_provider_for_tool`(各プロバイダの`list_tools()`が返す名前と
突き合わせるデータ駆動な方式。if/elifの連鎖ではない)を追加した。これにより、
将来`McpToolProvider`を追加する際は`TOOL_PROVIDERS`にインスタンスを足すだけで
組み込みツールと同列に扱えるようになる(実際のMCP接続自体は第6段階でやる、
ここでは受け皿だけを用意した)。

`exec_tool`自体は**公開シグネチャ・挙動を一切変えていない**薄いラッパーとして
残した——`ToolContext`を組み立てて`_provider_for_tool`で見つけたプロバイダへ
委譲するだけ。既存の呼び出し元(`handle_chat`)・既存テスト(`test_tool_normalization.py`)
は変更不要で、実際に既存93テストが無修正で全て通ることを確認している
(最も回帰リスクが高いと事前に見積もっていた項目だったため、後方互換性を
最優先にした)。新規`tests/test_tool_provider.py`で`BuiltinToolProvider`・
`_provider_for_tool`・`ToolContext`を直接検証。実チャットで`write_file`→
`read_file`→`run_command`の3種のツールを連続実行させ、新しいディスパッチ経路
でも正しく動作し、他機能(未検証変更の検知等)とも問題なく連携することを確認した。

物理的な`server.py`の複数ファイル分割(§8.1の残り、モジュール構成案は§8.1参照)は
今回のスコープに含めていない——「ツール実行部分の分離」はクラスによる論理的な
分離にとどめ、ファイル分割は保守・配布(第5段階)で別途行う。

**構造化ツール結果の設計ノート**（`IMPROVEMENTS.md` §4.1の実装、第3.5段階=MCP
対応前提の4番目、最後の1項目）: 当初(今日の早い時間)は「既存の`ERROR:`/`OK:`/
`exit_code=0`という文字列規約が`build_work_state`/`extract_changed_files`/
`track_tool_repeat`等の広範囲に定着しテスト済みで、書き換えリスクに見合わない」
として見送っていた。しかし§4.1の原文をよく読むと「現在の文字列結果に**加えて**、
内部では構造化データを保持する」「モデルには読みやすい文字列を渡し、サーバー側
では機械判定に構造化データを使う」とあり、**既存の文字列表現を置き換える話では
なく追加する話**だった。この理解に基づき、リスクを最小化した設計にした:

- `run_command`自体・`ToolProvider.call_tool`・`exec_tool`のいずれも**戻り値の
  型を一切変えていない**(すべて従来通り`str`を返す)。構造化データは
  `parse_command_result(result: str) -> dict`という新しい純粋関数が、既存の
  文字列表現(`"exit_code=N\n..."`/`"ERROR: command ...\n..."`)から**事後的に
  逆算**する形にした。実行経路そのものに構造化データを通す新しい配線を足す
  よりも、既存の文字列を壊さず後付けできるこの方式のほうが、`server.py`で
  最もリスクが高いと見積もっていた項目を実際には低リスクに倒せた。
- `build_work_state`の2箇所（直近コマンドのOK/失敗表示、同一コマンド連続失敗の
  警告）にあった素朴な`r.startswith("exit_code=0")`チェックを
  `parse_command_result(r).get("ok")`に置き換えた。挙動は完全互換
  (`ok`が`True`/`False`/`None`(不明な形式)の3値を返し、`not ok`は旧実装の
  「exit_code=0以外は全部失敗扱い」と同じ結果になる)。
- `handle_chat`のツール実行ループで、`run_command`の結果を`messages`へ追加する
  際に`meta`フィールド（`parse_command_result`の出力）を追記するようにした。
  `content`(モデル向け文字列)は一切変更せず、`meta`は新しい追加フィールドなので
  既存の`content`読み取り側(圧縮・要約・ダッシュボード等)には影響しない。
  永続化される会話履歴JSONにも自然に残るため、将来の
  `METACOGNITIVE_REPLANNING.md`の再評価パスなどが構造化データを直接読める。
- §4.1のJSON例にある`stdout`/`stderr`の分離、`duration_ms`は見送った。現在の
  `run_command`はstdoutとstderrを`"\n[stderr]\n"`という区切り文字列で1本の
  文字列へ結合済みで、分離しようとすると出力自体にたまたまその文字列が
  含まれるケースで誤動作しうる。実際に必要としている消費先
  (`build_work_state`)は`ok`/`exit_code`だけだったため、確実に導出できる
  範囲に留めた。

単体テストは`parse_command_result`(成功・失敗・タイムアウト・キャンセル・
未知形式・空文字列)と、`build_work_state`が構造化解析経由でも従来と同じ結果を
出すこと(OK表示・失敗表示・連続失敗警告・タイムアウト後の成功で警告が
リセットされること)を検証。実チャットで`echo hello`(成功)と`exit 1`(失敗)を
連続実行させ、`messages`に保存された`meta`フィールドが
`{"ok": true, "exit_code": 0, ...}` / `{"ok": false, "exit_code": 1, ...}`と
それぞれ正しく記録されることを確認した。

これで第3.5段階(MCP対応の前提)の5項目——現在のゴール専用フィールド・完了条件の
機械的管理・構造化ツール結果・ツール実行部分の分離・ToolProviderインターフェース
——が全て完了した。`IMPROVEMENTS.md`の第6段階(MCP接続)に進める状態になった。

**診断パッケージ出力の設計ノート**（`IMPROVEMENTS.md` §9.3の実装、第2段階の
残り項目）: 問題報告用に、秘密情報を除いた診断情報一式をJSONでダウンロードできる
`build_diagnostic_bundle`関数と`GET /api/diagnostic_bundle`(トークン必須、
`?sid=`と`?error=`をクエリで受け付ける)を追加した。含める情報:
LocalCoderバージョン(`SERVER_VERSION`)・Pythonバージョン・OS/platform情報・
起動時セルフチェック結果・主要な設定値(`NUM_CTX`等)・Ollamaバージョン・
モデル一覧(vision対応可否込み)・(sid指定時のみ)そのセッションの`turns`配列
(診断情報のみ、`messages`本文は含めない)・(error指定時のみ)報告されたエラー
文字列(2000文字で打ち切り)。**個人パス(`ALLOWED_ROOTS`の実パス・
`DEFAULT_WORKSPACE`)とCSRFトークン(`TOKEN`)は明示的に除外**した
(`ALLOWED_ROOTS`は件数のみ`allowed_roots_count`として含める)。フロント側は
ヘッダーに「🩺 診断」ボタンを追加し、クリックで`Blob`+`<a download>`によって
ローカルにJSONファイルとして保存する(サーバーに送信するのではなく、ユーザーが
バグ報告時に自分の判断で添付できるようにする形)。

単体テストでは、`urllib.request.urlopen`を呼び出し先URL(`/api/version`/
`/api/tags`)ごとに異なる応答を返すよう差し替える`_multi_response`ヘルパーを
新設して検証(既存の`run_self_check`テストの`return_value`単純固定では
複数エンドポイントを叩く関数を検証できないため)。特に、生成した診断
パッケージのJSON文字列全体に`TOKEN`や`ALLOWED_ROOTS`の実パスが**含まれて
いないこと**、`sid`指定時に`messages`本文(会話の秘密の内容)が含まれず
`turns`だけが入ることを直接アサートした。実ブラウザで、実際にOllamaへ
接続してバージョン・20個のモデル一覧を取得できること、生成されたJSONに
`window.LC_TOKEN`の値が含まれないこと、診断ボタンのクリックがコンソール
エラー無く完了することを確認した。

**差分中心の再読(キャッシュヒット判定)の設計ノート**（`IMPROVEMENTS.md` §6.3の
一部実装、第3段階の残り項目）: §6.3は本来「差分と周辺行を優先して渡す」という
diff生成まで含む設計だが、今回は「低リスクな地道な仕上げ」というスコープに
収めるため、その第一歩である**同一内容のキャッシュヒット判定**だけを実装した。
diffの生成・行番号ベースの部分再提示は今回のスコープ外とし、`IMPROVEMENTS.md`の
進捗表にもその旨を明記した(完全実装ではない)。

- 新しい純粋関数`find_previous_read(messages, path)`が、会話履歴
  (`_iter_tool_calls_with_results`経由)から同じ`path`を対象にした直近の
  **成功した**(`ERROR`で始まらない)`read_file`呼び出し結果を探す。他の
  機械的抽出関数(`extract_changed_files`等)と同じパターンで、LLMを介さず
  決定的に判定する。
- `ToolContext`に`messages: list | None = None`フィールドを追加した。
  `None`の場合(既存のテストや将来の呼び出し元)はキャッシュヒット判定自体を
  省略し、常に全文を返す(後方互換)。`exec_tool`のシグネチャにも
  `messages`を追加したが、キーワード専用の追加引数なので既存の位置引数・
  キーワード引数の呼び出しは無修正で動く。
- `BuiltinToolProvider.call_tool`の`read_file`分岐で、`ctx.messages`が
  与えられていれば`find_previous_read`の結果と新しく読んだ内容(60000文字
  切り詰め後)を**直接の文字列比較**で照合する。ハッシュ比較にしなかったのは、
  会話履歴側に前回の全文がどのみちメモリ上にあるため、ハッシュを介す必然性が
  ないため(SHA-256はあくまで「変化なし」メッセージの中で人間・モデルへの
  参考情報として使うのみで、判定そのものには使わない)。一致すれば全文の
  代わりに`(内容は前回read_fileした時から変わっていません。SHA256=...、
  N文字。前回の内容をそのまま参照してください)`という短いメッセージを返す。
- 比較対象は表示用に切り詰められた後の文字列(60000文字上限)にした。理由は
  単純さ(切り詰め前の全文をどこにも保持していないので、比較にはそもそも
  切り詰め後の文字列しか使えない)。**既知の制約**として、60000文字を超える
  ファイルで、表示される先頭60000文字は変わらず末尾側だけが変化した場合、
  誤って「変化なし」と判定してしまう。頻度・実害とも低いと判断し、ドキュメント
  化のみで対応とした。
- `handle_chat`のツール実行呼び出し(`exec_tool(...)`)に`messages=messages`を
  追加した。この`messages`はセッションの会話履歴全体(圧縮済みマーカーを含む
  場合がある)であり、圧縮でツール結果が要約に置き換わった後は
  `find_previous_read`が見つけられなくなる(＝安全側に倒れて全文を再送する)、
  という点も意図した挙動として確認した。

単体テストは`find_previous_read`単体(該当なし・成功結果を返す・他ツール/他
pathを無視・失敗結果を無視・複数回読んだ場合は最新を返す)と、
`BuiltinToolProvider.call_tool`経由での結合テスト(`messages=None`で従来通り
全文・内容一致でキャッシュヒットメッセージ・内容不一致で全文・初回読み込みは
全文)、`exec_tool`が新しい`messages`キーワード引数を受け付けることを検証。
実際のOllama(`gpt-oss:20b`)を使った実チャットで、同じファイルを2ターンに
分けて読ませ、1ターン目は全文(`hello e2e read cache test`)、2ターン目は
`(内容は前回read_fileした時から変わっていません。SHA256=ceeded8e0885e6fe、
26文字。...)`という短縮メッセージが返ることを確認した(検証用ワークスペース・
セッション・テスト用サーバープロセスは確認後にすべて削除済み)。

**MCPクライアント(`McpToolProvider`)の設計ノート**（`IMPROVEMENTS.md` §13 /
第6段階の実装）: 外部MCPサーバーのツールを、組み込みツールと同列にモデルへ
提供できるようにした。第3.5段階で用意した`ToolProvider`インターフェースの
2つ目の実装で、`TOOL_PROVIDERS`へ追加するだけで`_provider_for_tool`の
ディスパッチに乗るという当初の設計がそのまま機能した。

- **トランスポートはstdioのみ**(サーバーを子プロセスとして起動し、改行区切りの
  JSON-RPC 2.0で通信)。HTTP/SSEは実装しない。stdioの子プロセスはマシン内で
  完結するため、`REVERSIBLE_OPERATIONS.md` §8の外部送信ポリシーの対象外として
  扱える(§13.4の未確定事項の決着)。外部依存なし(subprocess+queue+threading、
  すべて標準ライブラリ)というLocalCoderの方針もそのまま守れる。
- **設定は`mcp_servers.json`**(一般的な`mcpServers`規約と同じ形式)。ファイルが
  無ければMCP機能は完全に無効で従来通り動く。個人パスを含むためgitignoreし、
  代わりに`mcp_servers.json.example`をコミットした(実データはこのマシンにのみ
  置く方針は`tests/fixtures/`と同じ)。
- **プロセス管理**: 遅延起動+`main()`での先行ウォームアップ(初回チャットの
  待ち時間と起動失敗の早期発見のため、起動時に一度`list_tools()`を呼んで
  `[OK]/[NG] MCP <name>: Nツール`を表示する)。パイプの読み取りは常駐リーダー
  スレッド+`queue.Queue`で行い、`queue.get(timeout=)`で応答待ちに上限を付ける
  (パイプ自体にはタイムアウトを付けられないため)。応答のid照合で他idの遅延
  応答・サーバー通知・非JSON行は読み飛ばす。ThreadingHTTPServerの複数スレッド
  からの呼び出しはプロバイダごとのロックで直列化。
- **段階的劣化**(§9.4の方針): 起動失敗時は`list_tools()`が空を返しモデルからは
  単に「そのツールが無い」ように見える。呼び出し失敗時は`ERROR: ...`文字列を
  返す(例外は漏らさない)。失敗したサーバーの再起動試行は`MCP_RETRY_INTERVAL`
  (60秒)に1回までとし、壊れた設定が毎リクエストに起動待ちの遅延を足すことを
  防ぐ。呼び出し中のハング・切断はプロセスをkillして次回呼び出しで再起動する。
- **名前衝突**: `all_tools()`(Ollamaへ渡すツール一覧の合算)は先に登録された
  プロバイダを優先して後発の同名ツールを除外する。`_provider_for_tool`も同じ
  順序で先勝ちするため、モデルに見せる定義と実際のディスパッチ先が常に一致する
  (組み込みが常に先頭なので、悪意ある/不注意なMCPサーバーが`run_command`等を
  名乗っても組み込みの実装が使われる)。
- **弱いローカルモデルへの配慮**: toolsスキーマを渡すだけだと追加ツールを使い
  忘れることがあるため、MCPツールが存在する時はシステムプロンプト末尾にも
  ツール名と説明1行を動的に追記する。
- **診断との統合**: セルフチェックに「MCP設定」項目(設定ファイルがある時のみ、
  JSONの妥当性とコマンドの存在を検査)、診断パッケージに`mcp_servers`
  (名前・生存・ツール数のみ。command/argsは個人パスを含みうるため入れない)を
  追加した。

最初の実利用サーバーとして PicoCalc Expert MCP(`~/pico_dvl/codex/mcp/`
`picocalc_expert.py`、別リポジトリ、同じくPython標準ライブラリのみ)を接続した。

単体テストは、フェイクMCPサーバー(テストファイル内に埋め込んだ数十行の
スクリプト)を実際に子プロセスとして起動し、本物のプロセス間通信でハンド
シェイク・ツール変換・isError・プロセス再利用・起動失敗の劣化・再試行抑制・
名前衝突(組み込み優先)・`exec_tool`経由のディスパッチ・設定ファイルの
読み込みを検証(16件追加、計146件)。実際のOllama(`gpt-oss:20b`)を使った
実チャットでは、`get_reference(topic='psram')`がMCP経由で呼ばれ、コーパスの
正本文書`general/04_CLOCK_MEMORY_PSRAM.md`の見出し一覧が返り、モデルがそれを
正しく要約することを確認した。診断パッケージにトークン・MCPサーバーの個人パスが
含まれないことも実サーバーで確認した。

**自動方針再評価パスの設計ノート**（`METACOGNITIVE_REPLANNING.md` 第1〜2段階の
実装）: 実障害セッション(`history/mrm0pw3vs82iww.json`、PicoCalc向けテキスト
エディタ作成タスク)で、**74ツール呼び出し・72イテレーションを消費しながら
タスク本体のmain.cppを一度も作成せず**、「まず既存APIを確認してから作成します」
という宣言と確認だけを30回以上繰り返し、ユーザーが手動停止する事例が起きた。
エラーは1件も無く(`run_command`全成功・ERROR結果ゼロ)、既存の異常検知
(同一失敗の連続=`TOOL_STUCK_LIMIT`)では原理的に捕まらないタイプの停滞だった。
これが本機能の直接の動機であり、初期発火条件の妥当性検証の基準にもした
(このセッションと同型の合成シーケンスが「進捗なし8ツール」+「内容不変の
再読3回」=4点で発火することを単体テストで固定)。

設計の要点(いずれも`METACOGNITIVE_REPLANNING.md`の設計に忠実):

- **再評価は判定であって割り込みではない**: 「考え直せ」と促すのではなく、
  ツールなしの専用LLM呼び出し(`run_review_pass`)に現在状態の機械的要約
  (`build_review_context`)だけを渡し、`continue`/`adjust`/`change`/`stop`の
  4値+根拠・次の行動・期待結果・再評価期限を構造化JSONで出させる。
  `CONTINUE`も「期限付きの継続許可」という情報価値のある判定として扱い、
  §7の採用条件(根拠1件以上・具体的な行動・検証可能な期待・failure_means・
  有限のreview_after)を`validate_review_decision`で機械的に強制する。
  不備があれば1回だけ修正要求し、それでも駄目なら不採用(通常作業は壊さない)。
- **発火は単一条件ではなくスコア合算**(`review_score`、閾値4点):
  12ツール呼び出し(+2)/進捗なし8ツール(+2)/同一失敗2回(+3)/空応答回復(+3)/
  圧縮後4ツール(+1)/経過10分+進捗なし5ツール(+1)/内容不変の再読3回(+2)。
  最後の項目は設計書では第4段階(§11.8)だが、差分中心の再読のキャッシュヒット
  通知(`UNCHANGED_READ_NOTICE_PREFIX`)から機械的に判定できるため前倒しした。
- **進捗イベントの機械的判定**: LLMの申告は進捗と認めない。ファイル変更系
  ツール(`MUTATING_TOOLS`)の成功と、直前に失敗していた`run_command`の成功
  (=ビルド/テストの改善)の2種類だけ。読むだけの操作は何回成功しても進捗に
  数えない——上記実例では「確認の成功」だけが延々続いた。
- **連続発火の防止**(§13): 再評価後は最小6ツール空ける(`review_after`期限
  到達だけは優先)、1ターン最大3回、前回と同じ発火理由の組だけでは再発火
  しない(新しい兆候が加わるか期限到達が必要)。
- **保存とモデルへの提示を分離**(§3, §14): 採用結果はrole=`localcoder_meta`の
  メッセージとして履歴JSONに永続化するが、Ollamaへの入力からは
  `to_ollama_messages`で常に除外する(Ollamaは未知roleを受け付けないため、
  ここを分離しないと会話が壊れる)。モデルへは次の通常呼び出しの作業状態
  ダッシュボード末尾に「(自動方針再評価 - 現在の実行方針)」ブロックとして
  毎回再提示する(`format_review_for_dashboard`)。
- **STOP判定はターンを止める**(`review_stop`ステータス): 必要情報の欠如や
  解消不能な障害と判定された場合、無駄なループを続けずユーザーへ制御を返す。
  ちょうど上記実例でユーザー自身が「継続できなくなったら経緯を記録して中断して」
  とピン留め指示していた動きを、LocalCoder側の機能として提供する。
- **UI**: 方針変更時だけでなく`CONTINUE`を含む全判定を🧭カード(details)で
  表示する——「一度検討したうえで現在の方向を続けている」ことを確認できる。
  履歴を開き直した場合も`localcoder_meta`メッセージからカードを再構築する。
- **無効化と調整**: `LOCALCODER_STRATEGY_REVIEW=off`で機能全体を無効化。
  `LOCALCODER_REVIEW_SCORE_THRESHOLD`/`LOCALCODER_REVIEW_AFTER_TOOL_CALLS`で
  発火感度を調整できる(実測調整=第5段階の前準備を兼ねる)。
- 第3段階以降(前回予測との実績照合の自動化・エラー署名の正規化・節目発火・
  モデル別統計)は未実装。ただし前回判定と「その後の実績」(ツール数・進捗なし
  数)は§8の形式で再評価コンテキストに既に含めており、第3段階はその判定活用
  部分を追加する形になる。

単体テストは39件(`tests/test_strategy_review.py`): 進捗イベントの機械的判定
(変更系成功のみ・読み取りは対象外・失敗コマンドからの回復)、スコア表の各項目、
発火抑制(最小間隔・1ターン上限・同一理由の再発火禁止・期限優先)、JSON抽出
(コードフェンス・前置き付き)、採用条件(CONTINUEの必須項目群)、修正要求
リトライ、コンテキスト組み立て、`localcoder_meta`の形とOllama入力からの除外、
そして実障害セッションと同型のシーケンスが閾値に達することの固定化。実際の
Ollama(`gpt-oss:20b`)では、発火閾値を環境変数で下げたテストサーバーで
発火→gpt-ossが有効なCONTINUE JSONを一発で返す→SSEの`strategy_review`
イベント→履歴への`localcoder_meta`保存→次呼び出しのダッシュボード再提示、
の全経路を確認した。UIの🧭カードはnode上のDOMスタブで`reviewCard`を実行して
表示項目を検証した(ブラウザ実機はポリシー制約のため未実施)。

**追補: 前回判定のターン跨ぎ引き継ぎ**(初回リリース直後の実障害
`history/mrm3q667l6m92v.json`による修正): 初回実装はターン0で正しくSTOPを
発火させた(「情報収集14回・コード0行」を検知し、「次はCMakeLists.txtと
main.cの最小構成を1回で作成する」という具体的な計画まで残した)。しかし
ユーザーが次のターンで「実装作業を開始してください」と指示すると、モデルは
**前ターンのSTOP判定を一切踏まえずに同じget_reference×13回の調査ループを
繰り返し**、また同じ理由(many_tool_calls+no_progress)で発火した。原因は
2つの実装仕様の合成: (1) `ReviewState`はHTTPリクエストごとに新規作成される、
(2) `localcoder_meta`は`to_ollama_messages`でOllama入力から常に除外される。
つまり履歴に保存された判定を新しいターンで参照する経路がどこにも無く、
STOPの計画は誰にも読まれず埋もれていた。

修正: `find_last_strategy_review(messages)`(機械的抽出)と
`seed_review_state_from_history(rev, messages)`を追加し、`handle_chat`の
ターン開始時に前ターンの判定を`ReviewState`へシードする。設計上の要点:

- **期限の時計はターン開始から数え直す**(`last_review_at = turn_started_at`)。
  実際の`created_at`を使うと、古いセッションを何時間も後に再開しただけで
  `max_seconds`期限が即時発火してしまう。`review_after.tool_calls`も自然に
  「新ターン内でNツール後」の意味になる(実障害ならターン1の4ツール目で
  強制再評価が入り、13回の空回りの大半を防げた)。
- **`last_fire_reasons`(同一理由の再発火抑止)は引き継がない**。§13の抑止は
  同一ターン内の連続発火対策であり、引き継ぐと実障害のターン1で必要だった
  正当な再発火(前回と同じ理由の組)まで塞いでしまう。`reviews_done`
  (1ターン最大3回の予算)も同様に引き継がない。
- **前ターン由来の判定はダッシュボード上で明示ラベルを変える**
  (「前ターンの判定。…新しい指示と矛盾する場合は新しい指示を優先」)。
  新しいユーザー指示が前ターンの作業と無関係な場合に、古い方針が
  「現在の実行方針」として誤って提示されるのを防ぐ。
- あわせて`format_review_for_dashboard`とUIの🧭カードに`plan`(複数手順)と
  `completion_criteria`(完了条件)の表示を追加した。実障害ではSTOPの`plan`に
  核心の具体策が入っていたのに、next_stepしか表示対象になかった。

単体テスト11件を追加(計245件)。実Ollamaでは、スコア発火を閾値99で無効化した
テストサーバーに「前ターンのCONTINUE(期限=1ツール)を含む合成履歴」を投げ、
新ターンの1ツール目直後に`reasons=["review_after_due"]`で発火することを確認
——スコアでは発火し得ない構成なので、これはターン跨ぎシードが実際に機能した
ことの決定的な証拠になる(1時間前のcreated_atでも即時発火しないことも同時に確認)。

**未検証の変更を残したままの早期終了を防ぐ設計ノート**（ターン跨ぎ修正の直後の
実障害`history/mrm597jy640ydz.json`による追加修正）: ターン跨ぎ修正自体は
正しく機能し、モデルは自発的に「前回のセッションで参照情報の収集だけで終了し
実質的なコード生成が進んでいなかった」と述べて8ファイルを実際に作成した。
しかし今度は**タスクが完了していないのに、ツール呼び出し無しの平文
「残りのファイルを作成してビルドする」だけを返してターンが`completed`扱いで
終了する**という新しい停止パターンが起きた。main.cppは未作成、`run_command`
によるビルド検証は一度も無し、8ファイル中5ファイルが未検証のままだった。

これは方針再評価の対象外の失敗モードである——`should_review_strategy`は
ツール呼び出しループの内側でしか呼ばれないため、ツール呼び出し自体が
発生しない終了は原理的に検知できない。既存の`build_work_state`による
「⚠ 未検証の変更…完了したと報告しないこと」という警告は表示されていたはずだが、
モデルはそれを見落とすか無視してツール呼び出し無しで終えていた。

既存の空応答自動回復(`EMPTY_RETRY_LIMIT`)と同じ枠組みで、条件だけを変えた
`UNFINISHED_RETRY_LIMIT`(既定1)を追加した:

- 発火条件: ツール呼び出し無し **かつ** 本文が空でない **かつ**
  `find_unverified_changes(messages[1:])`が1件以上ある(既存の未検証変更検知を
  そのまま流用、新しい判定ロジックは増やしていない)
- 空応答時と異なり`compact_history(force=True)`は呼ばない——文脈枯渇が原因では
  なく、単にモデルが検証をやり忘れているだけのため、圧縮する理由が無い
- `UNFINISHED_RESPONSE_NUDGE`で「run_commandでビルド/テストを実行して検証する
  か、作業を最後まで続けてください」と具体的に促す
  (空応答用の`EMPTY_RESPONSE_NUDGE`とは別メッセージ、目的が違うため)
  - 1回だけ(既定)。何度も同じ理由で足止めせず、モデルが指示に従わなくても
    最終的には通常のターン終了(`completed`)に落ち着く
- 診断情報に`unfinished_retries`(`empty_retries`と対になるフィールド)を追加

単体テストは既存の`find_unverified_changes`のテストで判定ロジック自体は
カバー済みのため追加していない(この機能はhandle_chatのHTTPループ内の分岐
そのものであり、既存の空応答リトライと同じく単体テストの対象外という
プロジェクトの慣例に合わせた)。実Ollama(gpt-oss:20b)で、合成履歴に
「write_file成功・run_command検証なし」を仕込み、「ツールを使わず一文で
報告してください」と明示指示することで検証: モデルは最初read_fileを呼んだ後
ツール呼び出し無しの応答を返し、通知(未検証の変更が残ったまま…)とnudge
メッセージの履歴挿入を確認、その後モデルは自発的にrun_command等で検証作業を
再開し、最終的に`completed`で正常終了した(無限ループにならないことも確認)。

**方針再評価 第3段階(前回予測との照合)+エラー署名正規化の設計ノート**
(`METACOGNITIVE_REPLANNING.md` 第3段階全項目+第4段階のエラー署名のみ):
動機は2つの実障害。(1) `history/mrmlsgf87glpgh.json`では、SDKパスが
ワークスペース外にあるため`list_dir`/`read_file`が「path is outside the
workspace: <毎回別のパス>」で**17回**失敗したが、合間に書き込みが時々成功する
ため`no_progress`カウンタがリセットされ続け、完全一致ベースの連続失敗検知にも
乗らず、再評価は3回ともCONTINUEのままmax_iter(80イテレーション)まで走った。
(2) 同セッションの2回目のCONTINUEは「前回判定も同様に…continueと判断された点」
を根拠に挙げており、前回判定を検証せず自己強化するだけになっていた。

- **エラー署名正規化**(`error_signature`, §11.4): エラー結果の先頭行から
  パス(unix/Windows)と数値を`<path>`/`<n>`へ置換した署名を作り、採用済み
  再評価ごとの窓で数える。最多署名が3回で+3点(発火理由`same_error`)。
  run_commandの失敗はexit_code行が全部同じ署名になってしまうため、本文中の
  最初の「error」を含む行を署名にする(異なる根本原因が同じ「cmake configure
  失敗」に集約されることがあるが、反復シグナルとしてはむしろ好都合)。
- **進捗イベントの記録**(§10): 真偽値だったものを`progress_events`リスト
  (`file_changed`/`command_recovered`、発生時点付き)に昇格し、採用済み
  再評価ごとにリセット。「前回判定後の実績」(§8)の機械的な材料になる。
- **前回判定後の実績要約と中心質問**(§8): 再評価コンテキストに、前回の
  理由・期待に加えて「その後の実績: ツール呼び出しN回 / 進捗イベントX件
  (内訳) / 同種エラーの反復N回」と、中心質問「前回この方針を継続または
  変更すると判断した根拠は、その後の実績によって支持されましたか」を明記。
  出力JSONに`previous_review: {exists, prediction_met, details}`を要求し、
  ダッシュボードと🧭カードに「前回の期待の検証: 実現した/しなかった」を表示。
- **進捗なしで同じCONTINUEを禁止**(第3段階の中核、機械的強制):
  `validate_review_decision`にstateを渡し、前回の採用済み判定がCONTINUEで
  それ以降の進捗イベントが0件なのに再びCONTINUEなら、自己申告の完成度に
  関係なく不採用+修正要求(adjust/change/stopへの変更を促す)。LLMの
  自己評価に任せず、進捗の有無という機械的事実で強制するのが要点。
- **発火予算のバグ修正**: 1ターン上限(`REVIEW_MAX_PER_TURN`)を採用数から
  **試行数**ベースへ変更した。採用数で数えると、JSON不良や上記の機械却下で
  不採用が続いた場合に上限が一生効かず、review_after期限が立ちっぱなしの
  まま毎ツール呼び出し後に再評価LLMが走り続ける(潜在バグ)。あわせて
  不採用に終わった試行の直後は、期限到達でも最小間隔(6ツール)を空ける
  クールダウンを追加した。

単体テストは16件追加(計261件): 署名の正規化(別パス同署名・数値・
run_command失敗のエラー行抽出・種類の弁別)、実障害パターン(書き込み成功が
混ざってもsame_errorが+3される)の固定化、進捗イベントの記録とリセット、
進捗なしCONTINUE却下(進捗があれば許可・前回がADJUSTなら許可・初回は許可)、
修正要求経由でのADJUSTへの格上げ、試行数ベースの上限、失敗後クールダウン。
実Ollama(gpt-oss:20b)では、前ターンのCONTINUE(期待: main.c作成)を仕込んだ
合成履歴+ワークスペース外の読み取りだけを指示するタスクで、発火→
`previous_review: {exists: true, prediction_met: false, details: "Previous
expectation that main.c would be created was not met; ..."}`という正しい
照合結果を含む**CHANGE判定**が採用されることを確認した(進捗ゼロでの
CONTINUE反復は起きなかった)。

**発火感度の実測調整の設計ノート**(§12「閾値や重みは実セッションの診断情報
から調整する」の適用): 第3段階リリース直後の実セッション
`history/mrnej9juovu4a0.json`で「介入が早すぎる」問題が観測された。ターン序盤
**11ツール呼び出し・約3分の間に再評価が2回**発火。1回目のスコア5の内訳は
`no_progress`(+2)+`empty_response_recovered`(+3)だったが、どちらの根拠も弱い:
空応答は推定2.8Kトークン(予算の1割強)時点の単なるモデル不調で、§11.6が
+3を与える前提の「文脈枯渇」ではなく(強制圧縮も実際は何も縮めていない=
`compact_count: 0`)、no_progressの8ツールも§2.1自身が正当と認める「タスク
序盤の必要な探索」を停滞と数えていた。さらにADJUST判定が、発火理由の一つ
だった存在しないツール`open_file`を「再度呼び出せ」と勧める品質問題もあった
(再評価LLMに使用可能なツール一覧を渡していなかった)。

調整3点:

- **ウォームアップ**(`REVIEW_WARMUP_TOOLS`=12、`LOCALCODER_REVIEW_WARMUP_TOOLS`
  で調整可): ターン開始からのツール総数(`total_tool_calls`、リセットされない
  専用カウンタ)が12未満の間は`no_progress`を加点しない。病的シグナル
  (`same_error`/`same_tool_failure`/`unchanged_reread`)はウォームアップ中も
  有効のまま——エラーの嵐は序盤でも本物なので探索猶予の対象にしない。
- **空応答回復の加点条件**: `note_empty_recovery(compacted=...)`に変更し、
  リトライ前の強制圧縮が実際に履歴を縮めた(=文脈圧迫が原因だった)場合のみ
  `empty_response_recovered`フラグを立てる。縮まなかった場合は既存のnudgeに
  任せ、再評価のシグナルにはしない。
- **使用可能なツール一覧**: `build_review_context`が`all_tools()`(MCPツール
  込み)の名前一覧を「これ以外のツールは存在しない」と明記して渡し、
  プロンプトにも列挙外のツール名を提案しない規則を追加。

この調整で当該セッションを机上再計算すると、11ツール目の発火は消え、
15ツール目前後で`same_error`(FileNotFoundError×3)+`no_progress`により発火する
——「探索には口を出さず、エラーの嵐が始まった時点で介入」という望ましい形に
なる。既存の実障害2件の検知タイミングはほぼ不変(74回停滞: 11→12ツール目、
17回エラー反復: same_errorは序盤でも有効なので変化なし)。単体テスト4件追加
(計265件)で、ウォームアップ中の抑制と通過後の加点・ウォームアップ中も
same_errorが有効なこと・圧縮を伴わない空応答が加点されないこと・ツール一覧の
明記を固定化。実Ollama(gpt-oss:20b)の既定閾値でのスモークでは、序盤の読み
取り+書き込み+検証という通常の短いタスクが**介入ゼロ**で`completed`する
ことを確認した。

**リプレイ分析ツール(`replay_review_triggers`/`tools/replay_review.py`)の
設計ノート**: ここまでの一連の方針再評価チューニング(第1〜3段階、感度調整)は
毎回、実障害セッションをその場でPythonスクリプトを書いて手動分析する
場当たり的な作業だった。「これからはチューニングのたびに過去の全実障害への
影響を機械的に検証したい」という要望を受け、`review_score`/
`should_review_strategy`/`error_signature`という**現在のロジック**を、
保存済みセッションの実データへLLM無しで通し直す仕組みを作った。

- **`replay_review_triggers(messages, turn_started_at)`**(server.py): 履歴の
  `messages`を発生順に走査し、`ReviewState.note_tool_result`を実際のツール
  呼び出し・結果で駆動しながら、各ツール呼び出し後に`should_review_strategy`
  を(現在のコードで)呼ぶ。発火した場合、その位置の直後に実際の
  `localcoder_meta`が残っていればその判定を`note_review`で「採用」したことに
  し(`review_after`による次の強制発火の連鎖を再現するため)、無ければ
  不採用扱いにする(`note_attempt_failed`)。**LLMは一切呼ばないため、判定の
  中身(CONTINUE/ADJUSTどちらが妥当か)は再現できない**——分かるのは
  「いつ・どのスコア・どの理由で発火するか」という機械的な部分だけ。これは
  意図的な範囲限定で、閾値・スコアリングのチューニングをOllama無しで検証する
  ための道具と割り切っている。戻り値の各イベントに`historical`(その位置に
  実際の判定があったか)と`adopted`(現在のロジックでも発火し採用できたか)を
  含め、「歴史的には発火していないが今は発火する」「今は発火しない」の両方を
  区別できるようにした。
- **`tools/replay_review.py`**: 上記+`error_signature`+
  `extract_changed_files`/`find_unverified_changes`をまとめ、ツール呼び出し
  ヒストグラム・エラー署名の反復回数・発火タイムライン・履歴上の実際の判定を
  一貫した形式で出すCLI。`python3 tools/replay_review.py <history.jsonへの
  パス>`。次に問題のあるセッションが来た時、一から解析スクリプトを書く代わり
  にこれを実行するだけで済むようにする狙い。
- **`tests/fixtures/review_incidents/`**(他のfixtures同様パス匿名化・
  gitignore対象): これまで分析した4つの実セッションを登録した——
  `runaway_no_progress`(前ターンのSTOPを踏まえ実際に8ファイル作成した成功例)、
  `early_stop_weaker_model`(ornith:35bが17回で素早くSTOP)、
  `workspace_boundary_errors`(17回のワークスペース外エラーでmax_iterまで
  検知されなかった実障害)、`early_intervention`(11ツール目での早すぎる介入)。
- **`tests/test_review_replay.py`**: 上記4フィクスチャそれぞれに、現在の
  コードでの発火タイミングを期待値として固定するテストを書いた。これにより
  以後`review_score`等を変更するたびに、`python3 -m unittest discover`一発で
  過去の全既知パターンへの影響を機械的に検証できる。

**この仕組み自体が発見した実例**: `early_intervention`フィクスチャの回帰
テストを書く過程で、感度調整コミット時に口頭で説明した見積もり
(「11ツール目の発火は消え、15ツール目前後でsame_errorにより発火する」)が
**不正確だった**ことが判明した。実際には`REVIEW_AFTER_TOOL_CALLS`(=12)と
`REVIEW_WARMUP_TOOLS`(=12)が偶然同じ値であるため、`many_tool_calls`が
ウォームアップ終了と同時に独立して加点され、結局12ツール目で(理由は
`no_progress`+`empty_response_recovered`から`no_progress`+`many_tool_calls`
へ変わるだけで)発火してしまう。手計算では見落としていたこの食い違いを、
`replay_review_triggers`を実装して初めて機械的に検出できた——場当たり的な
手動分析では気づけなかった具体例であり、この仕組みを作る動機そのものを
裏付ける結果になった(`test_review_replay.py`の
`TestEarlyIntervention.test_fires_at_warmup_boundary_not_optimistic_estimate`
としてこの事実自体を回帰テスト化してある)。

**書き込み系ツールの成功メッセージを解決済み絶対パスにした設計ノート**
(実障害`history/mrnfwve8nnr3t2.json`より、リプレイツール運用の初回実例):
このセッションでは方針再評価自体は完全に正しく機能した(3回の発火が
`replay_review_triggers`の計算と完全一致——`tests/fixtures/review_incidents/
stuck_relative_path.json`として登録し回帰テスト化した)。実際にターンを
止めたのは無関係な既存の`TOOL_STUCK_LIMIT`で、こちらも正しく機能していた。

真の原因は別にあった。ワークスペースが`/home/user`のセッションで、モデルが
`write_file`/`edit_file`/`list_dir`の`path`引数に**先頭の`/`が無い**
`home/user/project/test/demo2/CMakeLists.txt`という文字列を渡した。これは
ワークスペース起点の相対パスとして解決されるため、実際に書き込まれた場所は
ユーザーが指定した`/home/user/project/test/demo2/...`ではなく、1階層余分に
ネストした`/home/user/home/user/project/test/demo2/...`だった。ところが
当時の`write_file`は入力パスをそのままエコーするだけ(`OK: wrote N chars to
home/user/project/...`)だったため、モデルにも人間にも取り違えが見えず、
その後の`list_dir`も同じ間違ったパスで確認するため**自己整合的に「正しく
見える」**まま進行した。最終的に別の絶対パス(SDK)を同じ癖で誤って相対パス
化した際、今度は本当に存在しないパスで3回連続エラーとなり
`TOOL_STUCK_LIMIT`が発火してようやく停止した。

修正は`write_file`/`edit_file`/`delete_file`/`delete_directory`/
`move_file`/`copy_file`/`view_image`の成功メッセージと、`list_dir`の出力
先頭に、入力パスではなく`resolve_path`で解決した**絶対パス**を使うように
変更しただけ(1行〜数行ずつ、ロジックは変えていない)。これにより、相対
パスの解釈違いが起きた瞬間に結果メッセージ自体が矛盾を示し、モデルが
次の応答で気づける可能性が生まれる。`extract_changed_files`等は結果文字列
ではなくツール呼び出しの`args`を見るため影響を受けない。単体テスト5件
追加(`test_write_file_reports_resolved_absolute_path_not_raw_input`等、
計284件)。実Ollama(gpt-oss:20b)で`write_file`→`list_dir`を実行させ、
両方の結果に解決済み絶対パスが実際に現れることを確認した。

**「上限到達後の無監視」問題の設計ノート**(実障害`history/mrnnt9oripnrok.json`
より): 80イテレーション・80ツール呼び出しに達した(`max_iter`)長いターンを
`replay_review_triggers`で分析したところ、方針再評価は序盤の3回
(#12/#16/#20、いずれも現在のロジックと完全一致)しか発火せず、**残り60回の
ツール呼び出しが完全に無監視**だった。その60回の中身は、同じ約180バイトの
`main.cpp`への`write_file`を30回以上、一度も`run_command`で検証せずに
繰り返すだけの停滞。原因は2つ重なっていた:

1. `REVIEW_MAX_PER_TURN=3`という固定上限。§13「連続発火の防止」は同一ターン
   内のスパム抑止が目的で、ターンの長さに関わらず高々3回しか介入しないという
   意味ではなかったが、実装は後者になっていた。
2. `write_file`の成功は常に`tools_since_last_progress`をリセットする
   仕様だったため、同じファイルを検証せずに書き直し続けるだけでも
   `no_progress`が永遠に蓄積しなかった——上限が仮に無くても、この停滞
   パターン自体をスコアが捉えられなかった。

対策:

- **`REVIEW_MAX_PER_TURN`を3→8へ引き上げ**(`LOCALCODER_REVIEW_MAX_PER_TURN`
  で調整可)。`MAX_ITER`(80)と`REVIEW_MIN_INTERVAL_TOOLS`(6)から見て、
  長いターンでも間隔を空けて複数回チェックできる余地を持たせた。無制限には
  せず、上限自体は維持(スパム防止の趣旨は保つ)。
- **同一パスへの未検証な連続書き込みを進捗に数えない**: `ReviewState`に
  `unverified_write_paths`(`find_unverified_changes`と同じ「検証を試みたか」
  基準の集合)を追加。`write_file`/`edit_file`成功時、そのパスが既に
  未検証のまま変更済みなら進捗としてカウントしない(2回目以降は停滞判定に
  素通りする)。`run_command`の呼び出し(成否問わず、検証を試みたという事実)
  で集合をクリアし、次の書き込みはまた「新規」の進捗として扱う。
  `delete_file`/`delete_directory`/`move_file`/`copy_file`は「同じ内容を
  繰り返し書き直す」パターンではないため対象外のまま。`note_tool_result`に
  `args`引数を追加(省略時は従来通り常に進捗扱い、後方互換)。

`unmonitored_thrash`フィクスチャで確認すると、修正後は#12/#16/#20に加え
#24/#30/#36/#42/#48でも発火する(計8回=引き上げた上限まで使い切る)ように
なった。単体テスト8件追加(未検証連続書き込みの抑制・パス違いは独立に進捗・
run_commandによる集合クリア・args省略時の後方互換・edit_fileでも同様に抑制
されること・delete/move/copyは対象外のまま・上限の値そのもの)。既存の
`stuck_relative_path`フィクスチャにも旧上限では発生し得なかった4回目の
チェックが新たに現れることを確認し、回帰テストへ反映した。ライブの
Ollamaでは、人為的な「同じツールを30回繰り返せ」という指示にモデルが
素直に従わなかったため複数回発火の直接観測はできなかったが、実際の
過去セッションのツール呼び出し列を使った決定論的なリプレイの方が、この
種の機械的タイミングの検証としてはむしろ確実と判断し、そちらを主な
証拠とした(基本動作に crash が無いことは別途ライブでも確認済み)。

**「ゴールの言い換えによるすり替え」重大バグの設計ノート**(実プロンプト
`test/test2.txt`によるPicoCalcテキストエディタ新規作成タスクの実行中に発生):
ユーザーから、実際のプロンプトでLocalCoder+実Ollamaを動かし、`*.uf2`が
完成するかを監視するよう依頼された。実行したところ、モデルは**15ターン・
約3時間**にわたって、存在しない「PicoCalc Editor Demo」というプロジェクトを
探し続け(`find`/`search`/`list_dir`を数百回)、一度も完成に近づかなかった。
プロンプト自体には「他の完成済みPicoCalcアプリや既存のテキストエディタ実装は
参照しないこと」と明記されていたにもかかわらず。

セッションの圧縮マーカーを調べたところ、「現在のゴール」欄が次のように
破損していた:

```
PicoCalc LVGL Graphics Demo と test/demo2 の Minimal Editor Demo をビルドし、
UF2/ELF を生成する。
```

元のプロンプトは「demo2にPicoCalc向けの実用最小限のテキストエディタを
新規作成する」ことだけを求めていた。`picocalc_lvgl_graphics_demo`は
モデルが探索中に見つけた**無関係な既存プロジェクト**で、参照すら禁止
されていたはずのものである。

根本原因は`compact_history`の設計そのものにあった: 圧縮が起きるたびに
要約LLMへ「GOAL:」行を判定し直させ(`goal = new_goal or prev_goal`)、
新しい判定が得られればそれで**上書き**していた。探索中に見つけた無関係な
情報が要約の文脈に混ざり込むと、要約LLM自身がそれを「目標の一部」だと
勘違いして言い換えてしまい、その誤ったゴールが次の圧縮の入力(ダッシュ
ボード経由)になることで**自己強化的に固定化**する。方針再評価(§9の
「前回判定との照合」)はこの誤ったゴールを前提に「Editor Demoが見つからない
=停滞」と正しく判定し続けたが、ゴール自体が間違っているという可能性は
検証しようがなかった——`METACOGNITIVE_REPLANNING.md`のどの仕組みも
「ゴールの正しさ」までは検証範囲に入っていなかった。

これはこのリポジトリの一貫した設計原則(「重要な状態はLLMの記憶ではなく
機械的に保持する」)に反する箇所そのものだった。修正は原則に立ち返るだけ:

- **`extract_original_user_goal(messages)`**: ユーザーの最初の(実質的な)
  発言を、要約LLMに一切通さず、**言い換えずそのまま**ゴールとして機械的に
  取り出す。空応答/未完了応答の自動nudge、既存の圧縮マーカー自体はゴールと
  して扱わない。長すぎる場合は`GOAL_MAX_CHARS`(4000文字)で切り詰める
  (原文の先頭が保持されれば、参照禁止事項などの制約は通常そこに含まれる)。
- **`compact_history`**: `goal = prev_goal or extract_original_user_goal(old)
  or new_goal`。**一度確定したゴールは以後どの圧縮でも上書きしない**。
  要約LLMが依然として「GOAL:」行を出力することはあるが、`prev_goal`が
  存在する限り無視される。初回圧縮でのみ、機械抽出が万一失敗した場合の
  最終フォールバックとして`new_goal`(LLM判定)を使う。
- 失敗時(要約LLM呼び出し例外時)のフォールバックパスも同様に修正——
  以前は`prev_goal`のみを使っており、初回圧縮が失敗した場合にゴールを
  拾えない抜け穴があった。

単体テスト6件追加: 原文をそのまま返すこと、nudge/空文字列/既存マーカーの
スキップ、実質的なユーザーメッセージが無い場合の`None`、長文の切り詰め、
そして`compact_history`統合テスト(LLMが別のGOAL:行を出力しても2回目以降の
圧縮でゴールが変わらないことを直接検証)。既存の
`test_goal_extracted_and_carried_across_compactions`は新しい設計(不変性)に
合わせて書き直した(計301件)。

この破損したセッション自体(`history/real-test2-run1.json`)は、修正前の
コードで既に圧縮を繰り返した後の状態のため、元のプロンプトはメッセージ配列
からもはや失われており(圧縮マーカーの中にしか残っていない、しかもその
マーカー自体が破損の産物)、事後的な再検証はできない——これはまさに
この修正が防ぐべき症状そのものである。修正適用後、`test/test2.txt`を使った
新しいテスト実行で再検証する。

**再検証の結果**: ゴール破損修正後の新しいテスト実行では、存在しないプロジェクト
を探す迷走は再発せず、2ターン(前回の失敗より大幅に短縮)で初めて有効な`.uf2`
(RP2040ファームウェア、16,896バイト)の生成に成功した。ただしこの初回`.uf2`は
`stdio_init_all()`+`printf`+無限ループのみの最小スタブで、LCD表示・キー入力・
SDカード機能は未実装(プロンプト自身が定義する「最初のマイルストーン」のみ
達成)。続けてユーザー指示で実装を継続したところ、**別の重大な問題**を発見した:

**`run_command`のcwd(作業ディレクトリ)混乱の設計ノート**: `run_command`は
`subprocess.Popen(["bash", "-lc", cmd], cwd=ws, ...)`で呼び出しごとに
**完全に独立した新しいシェルプロセス**として起動され、`cwd`は常にワークスペース
ルート固定である。しかしモデルは、直前の呼び出しで`cd test/demo2/build`が
成功したことを理由に、次の呼び出しでもそのディレクトリに居続けると誤解し、
`cp -r ../PicoCalc/Code/picocalc_lvgl_graphics_demo/lib ./lib`のような
ワークスペースルート起点の相対パスでファイルをコピーし続けた。結果、
`lib/`・`i2ckbd/`・`lcdspi/`はすべて意図した`test/demo2/`ではなく
**ワークスペースルート直下**に着地し、`list_dir test/demo2`で30回以上
確認しても見つからないという停滞が発生、約1時間(4007秒)・80回のツール
呼び出しでMAX_ITERに到達した。方針再評価は8回発火し(引き上げた上限まで
使用)「進捗が無い」ことは正しく検知し続けたが、根本原因(cwdの誤解)までは
診断できなかった——これは第4段階(未実装)の「機械状態と判定の整合性検査」に
相当する、より高度な診断能力が要る領域。

修正は`TOOLS`の`run_command`説明文と`SYSTEM_PROMPT`の両方に、「毎回新しい
シェルが起動され`cd`は次の呼び出しに引き継がれないこと」「1回のコマンド内で
`cd dir && cmd1 && cmd2`のように連結するか、常にワークスペースルート起点の
パス(または絶対パス)を使うこと」「コピー/移動後は`list_dir`で実際の着地先を
確認すること」を明記しただけ(ロジック変更なし、プロンプト文言のみ)。この
セッションは既に進行中だったため、修正後は次回のサーバー再起動から反映される
(現在進行中のテストへの遡及適用はできない——ライブのセッションはPythonの
文字列定数として読み込まれたシステムプロンプトを使い続けるため)。

**圧縮前の生ログをコンテキストとは別に保存する設計ノート**(`archive_raw_messages`/
`tools/reconstruct_raw.py`): `compact_history`が古い部分を要約マーカーに
置き換えると、その生ログはどこにも残らない。実際に「最後のセッションを
検討して」と言われて`tools/replay_review.py`にかけたところ、圧縮で前半
(80回中最初の約60回)が要約に置き換わっており、方針再評価が何回目のツール
呼び出しで最初に発火したかを正確な位置で追えないセッションがあった
(集計値`turn.strategy_review_count`等は残るが、生の経過は失われている)。

修正は`compact_history`に古い部分を要約に置き換える直前のフック点を1つ足す
だけ:置き換えられる`new_raw`(前回アーカイブ済み分を除く新規増分)を、
`history/raw/<sid>.jsonl`へ1メッセージ1行で追記する(`archive_raw_messages`)。
モデルに渡す・UIに保存する側の`messages`は今まで通り圧縮され続けるので、
コンテキスト予算・応答速度への影響はゼロ。要約LLM呼び出しが失敗しても
生ログのアーカイブ自体は独立して行われる(要約とは別工程のため)。

分析時は`tools/reconstruct_raw.py <sid>`で「`history/raw/<sid>.jsonl`
(過去に圧縮された全分)」+「`history/<sid>.json`の現在の`messages`
(まだ圧縮されていない直近分、先頭が圧縮マーカーならそれは要約=劣化コピー
なので除く)」を連結し、`tools/replay_review.py`にそのままかけられる
完全な非圧縮ログを1つのJSONとして組み立てる。README.md「セッションログの
分析」セクションに、以後は圧縮の有無に関わらずこの2段階(reconstruct→
replay)を使う運用として明記した。`history/raw/`は`history/`配下なので
既存の`.gitignore`で自動的に除外される。

**no_progress判定に探索対象の再訪判定を導入した設計ノート(§14)**:
上記の生ログアーカイブ機能で最新セッションを分析したところ、ユーザーから
「作業方針の見直しがせっかちすぎる」という指摘があった。実際のセッション
(demo1へのPicoCalcテキストエディタ実装、参考資料はmcp/検索結果と
SDKフォルダの読み取りのみという課題の指示付き)を追うと、ツール#1〜11は
list_projects→mcp/・picocalc_devkit探索→ワークフロー文書→実装契約→
LCD正本文書→その正本が「移植せよ」と名指しした既存実装、と一貫して
新しい対象を掘り下げるだけの探索で、エラー0件・同一対象への再訪も0件
だった。それにもかかわらず#12でADJUST、#16(まさに移植元ファイルを
読んでいる最中)でSTOPが発火し、ターンごと打ち切られていた。

原因は`ReviewState.note_tool_result`の`no_progress`集計が、書き込み系
ツールの成功だけを「進捗」とみなし、読み取り系ツールは対象が毎回違って
いても一律に「足踏み」として数えていたこと。`REVIEW_WARMUP_TOOLS`(12)に
よる序盤の猶予はあったが、参考資料限定という課題では猶予期間そのものが
足りなかった。

ユーザーと相談し、「読み取り回数」ではなく「同じ対象への読み取りを
繰り返しているか」を見る設計(§14の呼称で以後参照)にした:
`_exploration_target(name, args)`が`read_file`/`list_dir`/`read_source`/
`search`/`get_reference`/`list_projects`の呼び出しから対象(正規化した
パス、または検索語/topic)を取り出し、`ReviewState`が前回の進捗イベント
以降に見た対象の集合(`exploration_targets_since_progress`)を保持する。
初めて見る対象への読み取りは`tools_since_last_progress`を進めない
(=`no_progress`にも`long_elapsed`にも数えない)。同じ対象への再訪、
または対象を特定できない呼び出し(引数無し等、安全側にフォールバック)は
従来通りカウントする。進捗イベントが起きたら対象集合はクリアされる
(その対象について何か変わった可能性があるため)。

パスの正規化(`_normalize_path_for_novelty`)は`./demo2`・`demo2`・`/demo2`
のような表記ゆれだけを同一視する軽量処理で、`resolve_path`のような
ワークスペース基準の実解決はしない(この関数はワークスペースを知らない
純粋関数のまま保つ)。既存の実障害フィクスチャ(`tests/fixtures/
review_incidents/unmonitored_thrash.json`)で、この正規化が無いと
`/a/b`→`./a/b`→`a/b`という言い換えの繰り返し(本物の足踏み)が「毎回
新しい対象」に見えてすり抜けることを確認した上で追加した。`read_source`/
`read_file`は`start_line`/`end_line`も対象に含める——同じファイルの
続きのページを読むのは再訪ではなく前進のため。`search`/`get_reference`は
query/project/kind、topic/sectionで区別する。`write_file`等の書き込み系
ツールは探索の対象外(従来通り、失敗すれば毎回カウント)。

この変更で7件の実障害フィクスチャ回帰テスト(`tests/test_review_replay.py`)
のうち5件の期待値がずれた——いずれも「序盤の新規探索がno_progressに
数えられなくなった」ことによるもので、`same_error`/`same_tool_failure`
(探索猶予の対象外の病的シグナル)が効く箇所は変わらず検知できることを
`tools/replay_review.py`で個別に確認し、テストを新しい(より正確な)
期待値に更新した。特に`early_stop_weaker_model.json`(17ツール・エラー0件・
再訪0件)は元々介入自体が「早すぎた」実障害だったと判明し、以後は一切
発火しないことをテストで固定化した。単体テスト14件追加、計314件。

ライブOllamaでのe2e再検証はしていない(スコアリングロジックの変更を
7件の実セッションフィクスチャ+単体テストで検証したのみ)。次回の実プロンプト
テストで挙動を確認する予定。

**履歴のリネームと送信ボタンの非活性表示の設計ノート**: UI改善2件。
(1) サイドバーの履歴項目をcodex/claude同様に右クリックでリネームできる
ようにした。`index.html`に`.sess`の`oncontextmenu`→軽量なコンテキストメニュー
(`.ctxmenu`、1項目「✏️ 名前を変更」)→タイトル部分をインラインの`<input>`に
差し替え、Enter/blurで確定・Escapeで取消。サーバー側は`POST /api/session/rename`
{sid,title}を新設し、`history/<sid>.json`の`title`を直接書き換えるだけ
(`save_session`の`existing_title`優先ロジックにより、以後の保存でも上書き
されず定着する——設計は既存のまま変更不要だった)。
(2) LLM実行中は`#sendBtn:disabled`にCSSを追加し、明示的にグレー背景・
`cursor:not-allowed`にした(`setRunning()`は既に`disabled`属性を切り替えて
いたが、カスタムテーマ下でブラウザ既定の非活性表示が視認しづらかった)。

検証: 実サーバー(`python3 server.py`)をテスト用に起動し、Browser paneの
JS実行で右クリック→メニュー表示→リネーム確定→ページ再読み込み後も
サーバー側に定着、Escapeでの取消、送信ボタンの非活性時のCSS(背景色・
`cursor`)を確認した。誤ってリネームした実セッションのタイトルは、`history/raw/<sid>.jsonl`
(前述の生ログアーカイブ機能)から元のゴール文を`derive_title`で再導出して
復元した——
検証中に自分の操作でデータを壊しても、その日実装したばかりの生ログ保存
機能で正確に復旧できることを図らずも確認した形になった。テスト自動化は
無し(HTTPハンドラ層は既存コードでも単体テスト対象外——`/api/session/delete`
等も同様——ブラウザでの手動/JS検証のみ)。

**thinkストリームの分析専用ログ化の設計ノート**(`log_thinking`/
`tools/show_thinking.py`): 実セッションで、SPI送信をPIOで書くかGPIO
ビットバンギングでやるかを「FINAL DECISION」と言っては何度も覆す独り言が
数千語続き、コードを一行も書かないまま空応答・強制圧縮を招いた疑いのある
実例があった。ところがこの独り言(Ollamaの`message.thinking`ストリーム)は
`amsg = {"role":"assistant","content":content}`の通り最終的な`content`
にしか残らず、`thinking`自体はSSEで一度クライアントに流した後は
`index.html`側でも表示用DOMごと`turn_done`で破棄されており、どこにも
保存されていなかった。ユーザーから「今後の解析用に生ログに残した方が
いいのではないか」との指摘を受けて対応した。

方針は生ログアーカイブ(`archive_raw_messages`)と同じ——**会話履歴
(`messages`、モデルへ再度渡る方)には含めない**。独り言をそのまま次の
文脈に含めると、今回のような巨大な独り言がさらに文脈を圧迫し悪化させる
だけなので、含めるのは分析専用ログ(`history/thinking/<sid>.jsonl`)だけに
した。メインループで`amsg`を`messages`に追加する直後の1箇所に
`log_thinking(sid, it, thinking, len(content))`を追加するだけの変更
(`thinking`が空なら何もしない)。1イテレーション1行のJSONLで、
`iteration`/`thinking`/`thinking_len`/`content_len`/`ts`を記録する。

分析用に`tools/show_thinking.py <sid>`を新設(一覧表示は長さ+冒頭200文字、
`--full`で全文、`--iteration N`で特定イテレーションのみ)。実Ollama
(`qwen3:8b`)で実際にthinkingを発生させ、`history/thinking/<sid>.jsonl`に
記録され`show_thinking.py`で読み出せることを確認した。単体テスト9件追加
(`tests/test_history_io.py`の`TestLogThinking`、`tests/test_show_thinking.py`)、
計320件。

**`empty_exhausted`ステータスの設計ノート**: 新設したthinkingログを使って
実セッション(gpt-oss:20b、簡略化した「テキストビューワー」課題)を分析
したところ、保存された記録が`status: completed`なのに、実際にはCMakeLists.txt
すら一度も作られておらず、45回のツール呼び出しの大半(42回)が探索で
終わっていたセッションが見つかった。原因は、空応答リトライ
(`EMPTY_RETRY_LIMIT=1`)を1回使い切ってもモデルがまた空応答を返した場合、
ループはただ`break`するだけで`turn_status`をターン開始時の既定値
`"completed"`のまま変えていなかったこと。ユーザーの指摘(「そこは切り分けた
方がいい」)を受けて、この経路専用に`turn_status = "empty_exhausted"`を
設定するようにした(`server.py`の空応答リトライ枯渇箇所、1行追加)。
`index.html`の`STATUS_LABEL`に`empty_exhausted:"空応答で断念"`を追加し、
`stuck`/`max_iter`と同じ警告色(`.summary.status-empty_exhausted`)で
表示する。

これは「生成物の中身の判定」ではなく
「ターンがどう終わったかという記録の正確さ」の話——実際に完了したのか、
単に力尽きたのかを開発者が一目で区別できるようにする、外形的な記録の
話なので対応した。HTTPハンドラ層(`handle_chat`)は既存コードでも単体
テスト対象外のため、実サーバーで正常系(通常の応答は引き続き`completed`
になること)のみ確認した——`empty_exhausted`経路自体は空応答がモデルの
挙動に依存し確実に再現できないため、コードレビューでの確認に留める。

**停止ボタンの即時フィードバックと取りこぼし対策の設計ノート**: 「停止を押したら
すぐ停止中に遷移してほしい、取りこぼしのないように必ず停止してほしい」との
要望を受けて調査した。既存の停止経路自体(`run_command`は0.5秒間隔で
`cancel.is_set()`をポーリング、メインループもOllamaストリーミング中に
継続的にチェック)は既に機能しており、`handle_chat`冒頭の`ev.clear()`と
`/api/stop`の`ev.set()`が競合する理論上の余地はあるが、`send()`内で
`setRunning(true)`の直後に同期的に`await post(...)`を呼ぶJSの実装上、
実際のクリック操作でこの競合を人間が踏み抜くことは事実上不可能と判断した。

一方で実際に取りこぼしうる経路が2つ見つかった: (1) `$("stopBtn").onclick`が
`post("/api/stop",...)`を`await`もエラー処理もせず投げっぱなしにしており、
POST自体がネットワーク的に失敗すればサーバーに一切届かず、ユーザーには
成否が分からないまま停止しない。(2) ボタン自体の見た目がクリック後も
一切変わらず、押した反応が無いように見える。

対応: `requestStop()`を新設し、クリック直後に同期的にボタンを「■ 停止中…」
+無効化してから`post`を`await`する。成功(`{"ok":true}`)なら、対象ターンが
見つからなかった場合(`found:false`、既に終わっている等)でもエラー扱いには
せず、実際のターン終了(`setRunning(false)`)まで「停止中…」表示を維持する。
POST自体が失敗(例外)した場合のみ、エラー通知を出してボタンを元の
クリック可能な状態に戻し、再試行できるようにする——ここが実質的な
取りこぼし対策になる。`setRunning(true)`(新しいターン開始時)は、前回の
「停止中…」表示が残っていれば通常表示に戻す。サーバー側`/api/stop`は
対象ターンが見つかったかを`found`フィールドで返すようにした(1行変更)。

実サーバーを起動し、Browser paneのJS実行で(1)クリック直後に即座に
「停止中…」+無効化されること、(2)`setRunning(false)`で通常表示に戻ること、
(3)POST失敗をシミュレートした場合にエラー通知が出てボタンが再度クリック
可能に戻ること、の3点を確認した。HTTPハンドラ層は単体テスト対象外のため
(既存方針通り)、`/api/stop`の`found`フィールドはcurlで直接確認した。

**「挨拶がゴールとして固定される」ゴール破損バグの設計ノート**: 実プロンプト
(PicoCalcテキストビューワー、`nemotron-3-super:cloud`)で新たなゴール破損の
実例が見つかった。会話は最初のターンが単なる挨拶「こんにちは」(ツール呼び出し
0件)で、2番目のターンで初めて本題(demo1配下にビューワーを作る)が始まった。
`extract_original_user_goal`は「最初の実質的なuser発言をそのまま返す」設計
だったため、この挨拶がそのまま不変のゴールとして確定してしまった。

本題は69回のツール呼び出しを伴う実質的な作業(CMake設定の試行錯誤等)として
進んでいたが、方針再評価はダッシュボードに表示され続ける「現在のゴール:
こんにちは」を参照し、#65で「'こんにちは'という目標への進捗がない」と
CHANGE判定、モデルはこれを受けて`/home/user/project/greeting.txt`(demo1の
外、作業対象外の場所)に「こんにちは」と書き込んで読み返し、#69で「greeting.txt
に'こんにちは'が含まれている、ゴール達成」というSTOP判定を引き出した。
本題の実装(壊れたままだったが)はそのまま放棄された。

修正は`extract_original_user_goal`に「次のuser発言までの間にツール呼び出しが
あったか」の判定を追加し、それを満たす発言(=実質的な作業に繋がった発言)を
優先してゴールに選ぶようにした。ただし一度もツール呼び出しに至っていない
(=まだ本当に雑談段階の)場合にNoneを返すと、`compact_history`側のフォール
バック`goal = prev_goal or extract_original_user_goal(old) or new_goal`で
LLM由来の`new_goal`(要約LLMの言い換え)がその場しのぎで採用され、それが
`prev_goal`として恒久的に固定されてしまう——ゴール破損を防ぐための関数
自体がLLM言い換えの侵入経路になっては本末転倒なので、ツール呼び出しに
繋がった発言が1つも無い場合は、機械的な最後の砦として(ツール呼び出しの
有無を問わない)最初の有効な発言をそのまま返すフォールバックを維持した。

実際の事故セッションを`tools/reconstruct_raw.py`で完全ログ化し、修正後の
`extract_original_user_goal`にそのまま通して「テキストビューワー」を含む
正しいゴールが返ることを確認した。パス匿名化した抜粋を
`tests/fixtures/greeting_before_real_task.json`として追加し(既存のfixture群
同様.gitignore対象)、回帰テストで固定化。単体テスト3件追加(既存5件も
新しい前提(ツール呼び出しの有無)に合わせて更新)、計323件。

**履歴サイドバーの「削除」をアーカイブ化した設計ノート**: 「表の履歴への操作は
削除ではなくアーカイブにして、アーカイブ内の履歴への操作を元に戻すと削除に
してください」との要望。従来の「✕」は`f.unlink()`による即時・取り消し不能な
削除だった。`history/archived/`という新しいサブディレクトリを追加し(`raw/`・
`thinking/`と同じ配置方針)、通常一覧の「✕(現在は🗄)」は
`Path.rename()`で`history/`から`history/archived/`へ移動するだけにした
——`list_sessions()`は`HISTORY_DIR`直下を非再帰globするため、移動するだけで
自動的に一覧から外れる(コード変更不要)。

アーカイブ一覧(`GET /api/sessions/archived`、サイドバー上部の🗄トグルで
切り替え)には「↩ 元に戻す」(`POST /api/session/unarchive`、
`archived/`から`history/`へ戻すだけ)と「✕ 完全に削除」(`POST
/api/session/delete`、こちらが唯一の本当の削除)の2つの操作を用意した。
`GET /api/session?sid=`と`POST /api/session/rename`は、通常・アーカイブの
どちらにあるファイルでも動くよう`find_session_file()`(両方のディレクトリを
探す小さなヘルパー)経由に変更し、アーカイブ済みセッションの閲覧・改名も
そのまま使える。

`history/raw/<sid>.jsonl`・`history/thinking/<sid>.jsonl`(分析専用ログ)は
アーカイブ・削除のどちらでも移動・削除の対象外とした——セッション本体が
アーカイブ/削除された後も、分析資料として独立に参照できる方が一貫している。

実サーバーを起動し、Browser paneのJS実行で(1)アーカイブでメイン一覧から
消えアーカイブ一覧に現れること、(2)アーカイブ中でも`GET /api/session`で
閲覧できること、(3)「元に戻す」でメイン一覧に復帰すること、(4)使い捨ての
テストセッションで「完全に削除」がconfirm経由で実際にファイルを消すこと
(`history/`・`history/archived/`のどちらにも残らない)、を実データ(実験後は
元の場所へ復元済み)で確認した。

**モデルの:cloudバッジの設計ノート**: README.mdの前提修正(「LocalCoder自身は
外部API/クラウドAIを呼ばないが、`:cloud`サフィックス付きモデルを選ぶと
Ollama自体が外部へプロキシする」)を受けて、既存の「👁 画像対応」バッジと
同じ仕組みで、選択中のモデルが`:cloud`かどうかを選ぶ前に気づけるようにした。
サーバー側の問い合わせは不要——モデル名の末尾が`:cloud`かどうかだけで判定
できるため、クライアント側の文字列判定のみで完結する(`updateCloudBadge()`、
`updateVisionBadge()`と同じ3箇所——`loadModels()`後・`model`の`change`・
`openSession()`後——から呼ぶ)。`:cloud`なら赤字で「☁ クラウド(外部送信)」、
それ以外は通常色で「🔒 完全ローカル」。実サーバーでモデル一覧を1つずつ
切り替え、`:cloud`系4種すべてで警告バッジが、それ以外(`gpt-oss:20b`等)では
ローカルバッジが正しく出ることを確認した。

**書き込み範囲(`write_root`)の設計ノート**: 「参照資料を読ませたい広いフォルダ
(例: `/home/fuyuki/pico_dvl/codex`全体)をワークスペースに指定すると、モデルが
成果物を意図した作業フォルダ(例: `test/demo1`)ではなくワークスペース直下や
その他の場所に作り始めてしまう」という実運用上の不満への対応。以前、同種の
逸脱(指定外ディレクトリへのビルド成果物の作成)についてユーザーから
「それを発見して是正する行為はローカルコーダーの範囲外」という指摘を受けており
(任意の自然言語指示からの逸脱検知はLocalCoderの外形パターンベースの機構では
原理的にできない、という合意事項)、今回はその逆——ユーザー自身が境界を
「機械的に強制」できる設定を用意するアプローチを取った。「読み取りは広く・
書き込みは狭く」がこの設計の核: `workspace`自体は従来通り読み取り境界のまま
(参考実装や既存コードを広く読ませる用途を壊さない)にし、任意の狭い
`write_root`(`workspace`の部分木)を書き込み系ツールにだけ追加で適用する。

実装は`resolve_path(ws, p, boundary=None)`への`boundary`引数追加が中心
(`boundary`省略時は`ws`自身になり、既存呼び出しは無変更で従来通り動く)。
`ToolContext`/`exec_tool`に`write_root`を追加で通し、`write_file`・`edit_file`・
`delete_file`・`delete_directory`は`boundary=write_root`で解決する。
`delete_directory`の「ワークスペース自身は削除できない」自己チェックも
`write_root`設定時はそちらを基準にするよう変更した(でないと`write_root`
自体を丸ごと消せてしまう)。`move_file`/`copy_file`は意図的に非対称にした:
`copy_file`はモデルが参考実装を`write_root`外からコピーしてくる既存の
ワークフロー(実際にPicoCalcタスクでも使われていた)を壊さないよう、srcは
`ws`全体のまま読み取り可能にし、dstだけ`write_root`に制限する。一方
`move_file`はsrcからファイルを取り除く(=実質的な書き込み/削除操作)ため、
src・dstの両方を`write_root`に制限する。`read_file`/`list_dir`はそもそも
`boundary`を渡さず、常に`ws`全体を読める(読み取り境界は変えない)。

`handle_chat()`では、リクエストボディの`write_root`(相対パス文字列、省略可)を
`resolve_path(ws, write_root_str)`(`boundary`省略=`ws`基準で検証)で解決し、
`ws`外を指していれば即座にエラーSSEを返す。存在しないフォルダを指していても
同様にエラーにする(モデルに「そのフォルダをまず作ってから」と気づかせるより、
ユーザー側の入力ミスとして即座に弾く方が安全と判断した)。有効なら
`SYSTEM_PROMPT`に書き込み制限の説明を追記し、モデルが制限に気づかないまま
書き込みに失敗し続けることを防ぐ。`write_root`はセッション保存(`save_session`)
にも含め、再開時にUIへ復元する(`workspace`と同じ扱い)。

UIには「作業フォルダ」欄の隣に「書き込み範囲」欄(省略可)を追加した。空なら
`write_root: null`を送り、従来通り`workspace`全体に書き込める。当初はテキスト
入力のみで用意したが、「作業フォルダには📁参照ボタンがあるのに書き込み範囲は
手入力のままではUIとして統一感が無い」という指摘を受け、同じ`GET /api/browse`
+モーダルを書き込み範囲側にも共用する📁選択ボタンを追加した。既存の
`openBrowse()`/`browseSelect`ハンドラに`browseTarget`("workspace"か
"writeRoot")を1つ足すだけで、どちらのボタンから開いたかに応じてモーダルの
見出し・開始ディレクトリ(書き込み範囲側は現在の`write_root`値、無ければ
`workspace`値)・選択確定先を切り替える設計にし、モーダルや`/api/browse`自体は
複製しなかった。実サーバー+Browser paneで、書き込み範囲側のボタンから開くと
見出しが「📁 書き込み範囲を選ぶ」に変わり作業フォルダを起点に一覧されること、
サブフォルダ選択→確定で書き込み範囲欄にのみ反映され作業フォルダ欄は
変化しないことを確認した。

**方針再評価の「直近のツール呼び出し」要約が`read_file`の中身を見落とす
バグの設計ノート**: 実プロンプト(PicoCalcテキストビューワー、
`nemotron-3-super:cloud`)の最新セッション(`mrruhws8kafccg`)を分析していて
発見した。表面的な症状は、`test/demo1/CMakeLists.txt`が約20回書き直され、
参照実装からの再コピーによる自己後退も複数回起き、cmake configureが
成功したのがセッション最後のイテレーション(3ターン計239回のツール呼び出し
の最後)で、実際のビルド(make/ninja)には一度も到達できなかったこと。

`localcoder_meta`の方針再評価ログを追ったところ、「CMakeLists.txtに
`cmake_minimum_required`しか無い」という趣旨のADJUST判定が8回連続していた。
しかし実際の`read_file`結果(msg 371)を直接確認すると、`project(text_viewer)`・
`pico_sdk_init()`・`add_executable`・`target_link_libraries`まで既に
正しく書かれていた。原因は`build_review_context`の「直近のツール呼び出し」
欄([server.py:2790](server.py)付近)が、ツール結果を一律
`result.splitlines()[0][:80]`(先頭行だけ、80文字まで)に圧縮していたこと。
`write_file`の"OK: wrote N chars to ..."や`run_command`の"exit_code=N"の
ような結果は先頭行が本質情報そのものなのでこの圧縮で十分機能するが、
`read_file`の生の中身は先頭行に代表性が無い——ソースファイルの1行目は
`cmake_minimum_required(...)`のような定型句であることが多く、実質的な
内容(project定義やターゲット定義)は後続行にある。この結果、方針再評価は
「ファイルの中身は変わっていない/進捗が無い」という誤った状態認識のまま
ADJUST判定を出し続け、モデルはそのフィードバックに従って既に書いた内容を
無駄に書き直し続けるループに陥っていた(review自体が事実を誤認していた
という点で、「モデルの能力不足」とは別種の、LocalCoder側の状態要約の
設計不備)。

修正は`read_file`の結果だけを特別扱いし、`ERROR`/`(`(差分キャッシュの
短い通知)/`OK`で始まらない(=生のファイル内容とみなせる)場合に限り、
改行を空白1つに潰した上で文字数・行数を先頭に添えて150文字まで1行に収める
形式にした(`(N文字/M行) <内容の先頭150文字を1行に潰したもの>`)。それ以外の
ツール、および`read_file`のエラー・差分キャッシュ通知は従来通り先頭行
(80文字まで)のまま——`write_file`/`run_command`側の挙動を変えると却って
既存の(概ね機能している)要約の見やすさを壊しかねないため、影響範囲を
`read_file`の生内容のケースだけに絞った。単体テスト4件
(`test_read_file_result_is_not_reduced_to_first_line`ほか)を
`tests/test_strategy_review.py`に追加、計344件。実際の事故セッションの
メッセージ列(圧縮による切り詰めを一部含む実データ)に対して修正後の
`build_review_context`を再生し、`project(text_viewer)`が要約に現れる
ことを確認した。

単体テストは`tests/test_tool_provider.py`に`TestResolvePathBoundary`(`boundary`
引数そのものの挙動)と`TestWriteRootRestriction`(各ツールが`write_root`設定時に
正しく制限され、未設定時は従来通り`ws`全体で動くこと、および`copy_file`/
`move_file`の非対称性)を追加、計16件(既存340件から)。実サーバー+Browser paneで、
`workspace`外を指す`write_root`の即時拒否・存在しない`write_root`の拒否・
有効な`write_root`でのリクエスト正常受理(以降はOllama側の応答待ちのみで
write_root検証自体は通過)を確認した。

---

## 3. ファイル一式

server.py、index.html、LocalCoder.ps1、vendor、tests、tools、設定例、文書は、
同一コミットのリポジトリにある実ファイルを正本とする。この文書にはソース全文を複製しない。
文書内のコピーが実装から遅れる事故を防ぐためである。

別PCへ移植する場合は、Gitで同じコミットを取得するか、リポジトリの追跡対象ファイルを
ディレクトリ構造を保ったまま localcoder ディレクトリへコピーする。history、server.log、
__pycache__、mcp_servers.json、.localcoder は実行時データまたは個人設定なので移植元から
無条件にコピーしない。MCPを使う場合は mcp_servers.json.example を基に移植先で設定する。

ソース配置後、必要に応じて次のWindowsランチャーを作成する。

### Windowsランチャー LocalCoder.bat

2種類ある。**mirroredネットワークが使える機種は「標準版」で十分**（このリポジトリの
動作確認PCも標準版を使用中）。**WSLがNAT構成（mirrored非対応）の機種、または標準版が
ダブルクリックしても無反応になる機種**は「ASCII安全版＋LocalCoder.ps1」を使う。

**標準版**（mirroredネットワーク機。「1-2」のmirrored設定が入っていれば動く）:

```bat
@echo off
rem LocalCoder — ローカルLLM(Ollama)コーディングエージェント起動
rem サーバーが既に起動していれば二重起動しない(server.py側で処理)
start "LocalCoder Server" /min wsl -d ubuntu-24.04 -- bash -lc "python3 ~/localcoder/server.py"
ping -n 3 127.0.0.1 >nul
start "" "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --app=http://localhost:8765/
```

**ASCII安全版＋`LocalCoder.ps1`**（NAT構成機、または日本語コメント入りbatが
ダブルクリックしても何も起きない機種向け）:

⚠ **bat本体はASCII文字のみで書くこと。** 日本語コメントをUTF-8で書くと、
日本語版Windowsの`cmd.exe`（既定でShift-JISとして解釈する）がバイト列を誤読し、
コメントの途中を別コマンドとして実行しようとして壊れる（実際に発生した事例は
「8. 実施例ログ 6番」参照）。

```bat
@echo off
rem LocalCoder launcher (local-LLM coding agent)
rem NOTE: keep this file ASCII-only; cmd.exe misparses UTF-8 Japanese comments
rem Wake the WSL distro first so the \\wsl.localhost UNC path is reachable
wsl -d ubuntu-24.04 -- true
rem Copy LocalCoder.ps1 to a local path before running it. PowerShell's
rem RemoteSigned policy requires a digital signature for unsigned scripts
rem on network/UNC paths (\\wsl.localhost\...), even though local unsigned
rem scripts are allowed. Running a local copy avoids that restriction
rem without weakening the execution policy. Copying on every launch keeps
rem the repo's LocalCoder.ps1 as the single source of truth (no manual sync).
copy /Y "\\wsl.localhost\<distro>\home\<user>\localcoder\LocalCoder.ps1" "%TEMP%\LocalCoder.ps1" >nul
powershell -NoProfile -File "%TEMP%\LocalCoder.ps1"
```

`LocalCoder.ps1`（リポジトリ同梱）はゲートウェイIPの自動検出→必要ならOllamaをそのIPで
再bind→WSL内でserver.py起動→Edgeでアプリウィンドウを開く、を一括で行う
（Windows 10 + NAT構成のWSLでOllamaへの経路を確保する実装。詳細は「8. 実施例ログ」）。
mirrored機ではこの経路は不要。

⚠ **UNCパス上のps1は`RemoteSigned`ポリシーでも実行がブロックされる。** `powershell -File`に
直接`\\wsl.localhost\...`のパスを渡すと、「デジタル署名されていません。このスクリプトは
現在のシステムでは実行できません」というエラーで失敗する（`@echo off`のため一瞬で
ウィンドウが閉じ、症状としては「ダブルクリックしても何も起きない/黒い画面がすぐ消える」
に見える）。原因はローカルファイルとネットワークパス上のファイルでセキュリティゾーンの
扱いが異なるため。`-ExecutionPolicy Bypass`で回避する方法もあるが、ポリシーの緩和を
伴わない上記の「`%TEMP%`へコピーしてから実行」の方が望ましい（実際に発生した事例は
「8. 実施例ログ」参照）。

### vendorの再取得が必要な場合

marked と DOMPurify をバージョン固定でダウンロードして配置する。`server.py`が
`/vendor/*.js` として静的配信し、`index.html`はCDNではなくこのローカルパスを
参照する（「7. 設計上の注意」10番参照）。

```bash
mkdir -p ~/localcoder/vendor && cd ~/localcoder/vendor
# marked (UMD/minified build)
curl -sL -o marked.min.js "https://cdn.jsdelivr.net/npm/marked@18.0.5/lib/marked.umd.min.js"
echo 18.0.5 > marked.version
# DOMPurify
curl -sL -o purify.min.js "https://cdn.jsdelivr.net/npm/dompurify@3.4.11/dist/purify.min.js"
echo 3.4.11 > dompurify.version
```

バージョンを上げる場合はこのコマンドのバージョン番号を書き換えて再取得するだけでよい
（`*.version`ファイルは記録用でserver.py/index.htmlの動作には使われない）。

---

## 4. 検証手順

構築後、この順で確認する:

```bash
# (WSL内で)
# 1. サーバー起動
nohup python3 ~/localcoder/server.py > ~/localcoder/server.log 2>&1 &

# 2. ヘルスチェック
curl -s http://localhost:8765/api/health          # → {"ok": true}
curl -s http://localhost:8765/api/models          # → モデル一覧が返る
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8765/vendor/marked.min.js  # → 200
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8765/vendor/purify.min.js  # → 200

# 3. CSRFトークンの取得（起動ごとにランダム。index.html配信時に埋め込まれる）
#    POST系APIは全てこのトークンを X-LocalCoder-Token ヘッダで要求する（無いと403）
TOKEN=$(curl -s http://localhost:8765/ | grep -o 'LC_TOKEN="[a-f0-9]*"' | cut -d'"' -f2)
echo "token=$TOKEN"

# 4. エージェントのエンドツーエンドテスト（ワークスペースは $HOME 配下でなければならない）
mkdir -p ~/lc_test
cat > /tmp/lc_req.json <<EOF
{"sid":"test1","model":"gpt-oss:20b","workspace":"$HOME/lc_test",
 "messages":[{"role":"user","content":"hello.py というファイルを作って hello world と出力するようにして、実行して確認して"}]}
EOF
curl -s -N -X POST http://localhost:8765/api/chat \
  -H 'Content-Type: application/json' -H "X-LocalCoder-Token: $TOKEN" \
  --data @/tmp/lc_req.json | tail -5
cat ~/lc_test/hello.py   # → print("hello world") ができていれば合格
```

期待される挙動: SSEで `tool_start`(write_file) → `tool_end` → `tool_start`(run_command) →
… → `all_done` が流れ、hello.py が実際に作成・実行される。トークン無しでPOSTすると
`{"error": "forbidden"}` (403) が返るのが正しい挙動（動作確認ではない、CSRF対策の効果確認）。
（初回はモデルロードで1〜3分かかる。`python`が無ければ`python3`に自動で切り替えるなど、
エラー自己回復が観察できれば完璧）

最後に LocalCoder.bat をダブルクリックし、GUIウィンドウが開いてモデル一覧が
表示されることを確認する。

### 4-1. 自動テスト（Ollama不要・純粋関数のみ）

`tests/` に、履歴圧縮・作業状態ダッシュボード・ツール呼び出しの正規化・履歴
保存を対象にした回帰テストがある（`IMPROVEMENTS.md` §2.1〜2.2の実装）。
標準ライブラリの`unittest`のみで完結し、Ollamaへの接続もモデルのダウンロードも
不要（`ollama_ask`は`tests/_helpers.py`の`FakeOllama`で差し替える）。コードを
変更したら、サーバーを起動する前にまずこれを走らせる。

```bash
cd ~/localcoder
python3 -m unittest discover -s tests -t . -v
```

`tests/fixtures/` には実際に問題が起きたセッションのJSON（パスのみ匿名化した
実データ）を置く運用にしている。**`tests/fixtures/`自体は`.gitignore`対象**
（このマシンにのみ存在し、GitHubへは一切コミットされない）。実際の会話内容
（プロジェクトのコード片・やり取り本文）を含むため、パス匿名化程度では
リポジトリに含めるべきではないと判断した。fixtureが無い環境（`git clone`
直後など）では該当テストだけが`unittest.SkipTest`で自動スキップされ、
スイート全体は失敗しない（`tests/_helpers.py`の`load_fixture`参照）。

このマシンで運用している fixture と対応する不具合（参考。ファイル自体は
このリポジトリには含まれない）:

| fixture | 対応する不具合 |
|---|---|
| `leaked_special_token.json` | ツール名に特殊トークンが混入し呼び出しが永久に失敗 |
| `repeated_compaction.json` | 同一ツール結果の重複で圧縮が頻発しストール |
| `empty_response_near_budget.json` | 予算の99%まで伸びて空応答を繰り返す |
| `stuck_write_file.json` | `write_file`の`content`欠落を繰り返し連続失敗検出が発動 |

新しい実障害セッションが見つかったら、同じ要領で（このマシンの）
`tests/fixtures/`に追加し、その不具合を検知する回帰テストを`tests/test_*.py`
に足す。別のマシンで再構築する場合は、そのマシン上の実障害セッションから
同様に作成する。

---

## 5. 環境差分の調整（移植先で変わる箇所）

| 箇所 | このPCでの値 | 移植先での調整方法 |
|---|---|---|
| WSLディストロ名 | `ubuntu-24.04` | `wsl -l -v` で確認し bat の `-d` を変更 |
| デスクトップパス | `E:\desktop`（移動済み） | 通常は `%USERPROFILE%\Desktop`。PowerShellの `[Environment]::GetFolderPath('Desktop')` で確認 |
| 作業フォルダの初期表示値 | このPCでは`LOCALCODER_DEFAULT_WORKSPACE`環境変数で`/home/fuyuki/pico_dvl/codex`を指定 | 環境変数を移植先のプロジェクトパスに合わせて変更、または未設定のまま(`$HOME`が自動表示される) |
| Edgeのパス | `C:\Program Files (x86)\Microsoft\Edge\...` | 無ければ bat 最終行を `start "" http://localhost:8765/` に |
| Ollamaの場所 | Windows側 localhost:11434 | WSL内Ollamaでも同URLで可。別ホストなら環境変数 `LOCALCODER_OLLAMA` |
| ポート | 8765 | 競合時は環境変数 `LOCALCODER_PORT` |

## 6. カスタマイズポイント

- **ツール追加**: `TOOLS` にJSONスキーマを1個追加し、`exec_tool()` に分岐を1個追加するだけ
- **ループ上限**: `MAX_ITER = 80`、コマンドタイムアウト: `CMD_TIMEOUT = 180`
- **コンテキスト長**: `NUM_CTX = 32768`（VRAMが少ないPCでは16384に下げる）
- **システムプロンプト**: `SYSTEM_PROMPT` を編集（英語で書き「日本語で返答せよ」と指示するのが
  小型モデルには最も安定）
- **モデル選択の優先順位**: index.html の `pref` 配列

## 7. 設計上の注意（AIが再実装・改造する場合）

1. **Ollama native API (`/api/chat`) を使うこと**。OpenAI互換 `/v1/chat/completions` でも
   動くが、native APIは `thinking` フィールド（推論過程）が取れる。ストリーミングは
   JSON Lines形式で、`message.tool_calls` は途中チャンクに現れ、`done:true` で終端。
2. **tool結果メッセージ**は `{"role":"tool","tool_name":名前,"name":名前,"content":結果}`。
   `tool_name`(新)と`name`(旧)の両方を入れるとOllamaのバージョン差を吸収できる。
3. **`run_command` の出力は必ず切り詰める**（12KB上限、先頭6KB+末尾6KB）。
   小型モデルはコンテキスト溢れで即座に破綻する。
4. **会話履歴はクライアント保持**にするとサーバーがステートレスになり実装が単純。
5. **ファイル操作のパス検査**（workspace外拒否）は `resolve()` 後の文字列前方一致で行う。
   シンボリックリンク経由の脱出もこれで防げる。
6. **セキュリティ**: run_command はサンドボックスなしでユーザー権限実行（本人の希望による
   全自動設計）。127.0.0.1バインドだけでは**不十分**（同一PC上で開いている悪意あるWebページが
   `fetch("http://localhost:8765/api/chat", {mode:"no-cors", method:"POST", ...})` のような
   no-corsリクエストを送れてしまい、任意プロンプトでのコマンド実行につながる）。そのため
   起動ごとのランダムトークン（`X-LocalCoder-Token`）＋Origin/Host/Content-Type検証を
   全POST APIに必須化している（`_post_ok()`）。承認プロンプトを増やさずに閉じられる
   唯一の攻撃面がここなので、再実装する場合もこの層は省略しないこと。
7. **停止ボタンを機能させる**: `subprocess.run()`は結果を返すまでブロックするため、
   キャンセル要求が来ても実行中のコマンドを止められない。`Popen(start_new_session=True)`
   でプロセスグループを分離し、キャンセル/タイムアウト時に `os.killpg(pid, SIGKILL)` で
   子プロセスごと確実に終了させる（`run_command()`）。
8. **ワークスペースはホーム配下に制限**: リクエストの`workspace`値をそのまま信用すると
   `/etc`等を作業場に指定されうる。`ws.resolve()`した結果が`Path.home()`と一致または
   その配下であることを`handle_chat()`冒頭でチェックする。
9. **クライアント側のMarkdown描画は必ずsanitizeする**: LLM出力やWeb取得内容には
   HTML/`<script>`/イベントハンドラが混入しうるため、`marked.parse()`の結果を
   そのまま`innerHTML`に入れてはいけない。`DOMPurify.sanitize()`を必ず通す。
10. **依存JSはCDNから読み込まない**: `index.html`には`window.LC_TOKEN`（＝コマンド実行に
    到達できる権限）が埋め込まれるため、ページ内で動くJSは事実上その権限を持つ。CDN配信の
    JSはCDN側の改ざんや、バージョン無指定URLでの意図しない自動更新がそのままRCEに直結する。
    `marked`/`DOMPurify`は`vendor/`にバージョン固定でダウンロードして同梱し、`server.py`が
    `/vendor/*.js`として配信する（ファイル名は`[\w.-]+\.js`の完全一致のみ許可し
    パストラバーサルを防ぐ）。副作用として完全オフライン動作も実現する。
11. **履歴系GETにもトークンを要求する**: `/api/sessions`・`/api/session?sid=`は
    プロンプト・ツール結果・ファイル内容を含む機密情報を返す。127.0.0.1バインド＋
    通常のCORSだけでは、DNSリバインディング攻撃（攻撃者ドメインの名前解決を127.0.0.1に
    差し替える手法）でSOP判定そのものを回避されうるため、GETでもHostヘッダ検証を全体に
    かけ、履歴系だけは追加でトークンも要求する。`/`・`/api/health`・`/api/models`は
    トークン取得前に呼ぶ必要がありかつ機密を返さないため対象外でよい。

---

## 8. 実施例ログ: Windows 10 + GTX 1660 (6GB) への移植（2026-07-03）

AIエージェント（Claude Code）が本ドキュメントを読まずに、既存のLocalCoderセットが
置かれた別PCで「デスクトップから起動できるようにし、環境差分を吸収してほしい」と
依頼された際の実施記録。**`server.py` / `index.html` / README類は無改造**。追加した
ファイルは `LocalCoder.ps1`（リポジトリ内）と `LocalCoder.bat`（デスクトップ）の2つのみ。

### 環境

| 項目 | 元PC（README記載） | 移植先PC |
|---|---|---|
| OS | (未記載、Windows 11想定) | Windows 10 Pro build 19045 |
| GPU | RTX 3070 (8GB VRAM) | GTX 1660 (6GB VRAM) |
| WSLディストロ | `ubuntu-24.04` | `Ubuntu-20.04` |
| Ollamaモデル | `gpt-oss:20b` 推奨 | 未インストール（tool呼び出し非対応モデルのみ） |

### 詰まった点と解決

1. **mirroredネットワークが有効化できない** — `.wslconfig` に `networkingMode=mirrored`
   を書いて `wsl --shutdown` しても、WSL側は相変わらずNATのプライベートIP
   (`172.21.x.x/20`) のまま。原因はWindows 10であること（mirrored networkingは
   Windows 11専用機能）。`wsl --version` や `.wslconfig` の反映有無だけでは気づきにくいので、
   まず `[System.Environment]::OSVersion.Version` でWindows 11(Build 22000+)かを確認するのが早い。

2. **`OLLAMA_HOST=0.0.0.0` は自動化ポリシーで拒否された** — 「LAN全体にOllamaを公開する」
   変更として、明示的な許可なしにAIエージェントが実行することがブロックされた
   (Claude Codeの自動権限判定による)。ユーザーの追加承認を得た上で、代わりに
   「WSL専用の仮想アダプタIPだけにバインドする」方法（方法C）へ切り替えた:

   ```powershell
   # WSL側から見える"ゲートウェイIP" = Windows側 vEthernet (WSL) アダプタのIP
   $ip = (Get-NetIPAddress -InterfaceAlias "vEthernet (WSL)" -AddressFamily IPv4).IPAddress
   [Environment]::SetEnvironmentVariable("OLLAMA_HOST", "${ip}:11434", "User")
   # ollama.exe を再起動して反映
   ```

   これだけでは依然としてWindows Firewallに阻まれ、WSL側からの接続はタイムアウトした
   （`Get-NetFirewallRule` にOllama用の許可ルールが無かった）。**管理者権限**で
   インターフェース単位のスコープを持つ許可ルールを1本追加して解決:

   ```powershell
   New-NetFirewallRule -DisplayName "Ollama (WSL only)" -Direction Inbound -Protocol TCP `
     -LocalPort 11434 -Action Allow -RemoteAddress 172.16.0.0/12
   ```

   スコープは `-InterfaceAlias "vEthernet (WSL)"` ではなく **IP範囲で指定すること**。
   当初InterfaceAliasで作成したところ、Windows再起動でWSL仮想アダプタが再作成される
   とルールが古いアダプタ実体を指したままになり、無言で効かなくなった（ルールは
   Enabledのまま表示されるので気づきにくい）。`172.16.0.0/12` はWSL2 NATが使う
   プライベート帯で、OllamaはWSLゲートウェイIPにしかbindしていないため、これでも
   LANには公開されない。ファイアウォールルール追加はこの実施では管理者権限が無く
   自動実行できなかったため、コマンドをユーザーに提示して手動実行してもらった。

3. **推奨モデルが未インストール、既存モデルの大半はtool呼び出し非対応** —
   `ollama show <model>` の `Capabilities` に `tools` が出るかで判別できる
   （このPCでは `llama3` / `gemma3` / `qwen2.5vl` は非対応、`cogito` / 独自の
   `gemma4:e2b` は対応していた）。6GB VRAMを踏まえ `gpt-oss:20b`(約13GB)ではなく
   軽量な `qwen3:8b`(5.2GB, Q4)を新規pull。既存の `cogito:latest`(4.9GB)もtool対応の
   ため予備選択肢として使える。

4. **IPがPC再起動で変わりうる問題への対処** — ゲートウェイIPを起動のたびに動的検出し、
   Ollamaの実際のbind先とズレていれば自動で再bind＆再起動するロジックを
   `LocalCoder.ps1` に実装（詳細はスクリプト本体）。ファイアウォール側は
   `RemoteAddress 172.16.0.0/12` の範囲スコープなので、WSLサブネットが再起動で
   変わっても追従不要（前述の通りInterfaceAliasスコープは再起動で無効化するため不可）。

5. **セキュリティレビュー後の追加修正2巡** — 別レビュー（AIによる指摘）を受けて
   `server.py`/`index.html`を2段階で強化した。1巡目: POST全体へのCSRFトークン＋
   Origin/Host/Content-Type検証（`_post_ok`）、`run_command`のPopen+killpg化（停止
   ボタン実効化）、workspaceの`$HOME`配下制限、Markdown描画のDOMPurify sanitize。
   2巡目: 依存JS(`marked`/`DOMPurify`)をCDNからではなく`vendor/`に同梱配信、GET全体
   へのHostヘッダ検証、履歴系GET(`/api/sessions`・`/api/session?sid=`)へのトークン
   必須化。詳細は「7. 設計上の注意」6〜11番、コードは`server.py`/`SERVER.md`参照。

6. **バッチファイルの文字コード事故** — `LocalCoder.bat`に日本語コメント
   （`rem ローカルLLM(Ollama)コーディングエージェント起動`）をUTF-8で書いたところ、
   日本語版Windowsの`cmd.exe`（既定Shift-JIS解釈）がマルチバイト文字の後半バイトを
   コマンド区切りと誤認し、コメントの一部が「認識できないコマンド」として実行時
   エラーになった（ダブルクリックしても何も起きない、に見える）。UNC作業ディレクトリ
   （`\\wsl.localhost\...`）非対応の警告も別途出るため、原因切り分けには
   `cmd /c LocalCoder.bat` を対話的に実行してエラー出力を直接見るのが早い。
   対処: bat内は**ASCIIのみ**にし、`wsl -d <distro> -- true` を先頭に置いて
   WSLを明示的に起こしてからUNCパスを参照する（3節③参照）。

7. **ASCII化した後も直らなかった「黒い画面がすぐ消える」事故** — 6番を修正した後も、
   ダブルクリックすると黒いコンソールが一瞬出てすぐ消える症状が再発した。`@echo off`と
   `pause`無しのため原因が一切見えず、原因切り分け用に`pause`付き・`@echo on`のデバッグ版
   batを別途作って実行させたところ、実際のエラーが判明した:

   ```
   ファイル \\wsl.localhost\...\LocalCoder.ps1 を読み込めません。ファイル ...\LocalCoder.ps1
   はデジタル署名されていません。このスクリプトは現在のシステムでは実行できません。
   ```

   `Get-ExecutionPolicy -List`で`LocalMachine: RemoteSigned`を確認済みでも、**UNCパス
   （ネットワークパス）上の未署名スクリプトは、ローカルパスの未署名スクリプトとは
   異なるセキュリティゾーン扱いとなり、RemoteSignedポリシーでも実行がブロックされる**。
   `-ExecutionPolicy Bypass`で回避する手もあるが、ポリシーを緩めることになるため、
   代わりに**起動のたびに`LocalCoder.ps1`を`%TEMP%`（ローカルパス）へ`copy /Y`してから
   そちらを実行する**方式にした。ローカルパスなら未署名でもRemoteSignedで実行できる上、
   リポジトリ側の`LocalCoder.ps1`更新が次回起動時に自動反映されるため手動同期も不要。
   詳細は3節③のASCII安全版テンプレート参照。

   教訓として、`.bat`のトラブルシューティングは**最初から`pause`付き・`@echo on`の
   デバッグ版を作って実行させる**のが最短ルート。`@echo off`のまま原因を推測しても
   憶測が外れやすい（実際、最初は「他のウィンドウの裏に隠れているだけでは」という
   誤った仮説を挟んでしまった）。

### 最終構成（このPC固有、リポジトリ非同梱）

- `LocalCoder.ps1`（リポジトリ内に追加）: ゲートウェイIP自動検出 → Ollama再bind
  （必要な場合のみ）→ `wsl -d Ubuntu-20.04 -- bash -lc "... LOCALCODER_OLLAMA=http://<ip>:11434 python3 server.py"`
  → Edgeでアプリウィンドウを開く、を一括実行
- `LocalCoder.bat`（デスクトップ等）: `wsl -d <distro> -- true` でWSLを起こし、
  `LocalCoder.ps1`を`%TEMP%`へコピーしてから`powershell -NoProfile -File`で実行する
  薄いラッパー（`-ExecutionPolicy Bypass`は不要。**コメントはASCIIのみ**、UNCパス上の
  スクリプトを直接指定しない、の2点が必須。詳細は上記6・7番）

### 教訓

- 「移植先PCの前提を疑わずに手順書通りに進める」と、Windows 10かどうか・
  GPU VRAM量・WSLディストロ名の3点で必ずどこかに引っかかる。着手前に
  `[System.Environment]::OSVersion.Version` / `nvidia-smi` / `wsl -l -v` の3コマンドで
  前提差分を先に洗い出すと手戻りが減る。
- ネットワークを外部公開する変更（`0.0.0.0`バインド等）はAIエージェントが単独実行
  できない設計になっていることがある。行き詰まったら「スコープを狭めた代替案」
  （今回は特定インターフェースへのバインド＋ファイアウォールのインターフェーススコープ）
  を用意し、それでも管理者権限が必要な部分はユーザーに1行コマンドとして手渡すのが早い。

---

## 付録: 周辺環境（LocalCoderとは独立だが同時に構築したもの）

- **codex CLI 0.142.5**（WSL `~/.local/bin/codex`、`npm install -g --prefix ~/.local @openai/codex`）
  `~/.codex/config.toml` で組み込み `ollama` プロバイダ + `approval_policy="never"` +
  `sandbox_mode="workspace-write"`。注意: codex 0.142以降は `wire_api="chat"` 廃止、
  カスタム `[model_providers.ollama]` 定義も禁止（組み込みを使う）。
- **aider 0.86.2**（`~/.aider.conf.yml`: `model: ollama_chat/gpt-oss:20b`, `yes-always: true`,
  `set-env: [OLLAMA_API_BASE=http://localhost:11434]`）
